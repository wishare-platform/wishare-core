# This file loads all split locale files for en
# Original file backed up to en.yml.backup

en:
  # All translations are now in subdirectories
  # Rails will automatically load all .yml files in config/locales/**
