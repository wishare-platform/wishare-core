<% content_for :title, t('landing.title') %>
<% content_for :description, t('landing.description') %>

<div class="min-h-screen bg-gradient-to-br from-rose-50 via-amber-50 to-purple-50">
  <!-- Language Switcher -->
  <div class="fixed top-4 right-4 z-50">
    <div class="flex gap-2">
      <% available_locales.each do |locale| %>
        <%= link_to locale_switch_url(locale[:code]), 
              class: "flex items-center gap-2 px-3 py-2 rounded-lg text-sm transition duration-200 bg-white/80 backdrop-blur-sm border shadow-sm #{'border-rose-200 text-rose-700 bg-rose-50/80' if I18n.locale.to_s == locale[:code]} #{'border-gray-200 text-gray-600 hover:bg-white hover:border-gray-300' unless I18n.locale.to_s == locale[:code]}" do %>
          <span><%= locale[:flag] %></span>
          <span class="hidden sm:inline"><%= locale[:name] %></span>
        <% end %>
      <% end %>
    </div>
  </div>

  <div class="flex flex-col items-center justify-center min-h-screen px-6 py-12 sm:px-8 lg:px-8">
    <div class="text-center max-w-4xl mx-auto w-full">
      <!-- Logo and Brand -->
      <div class="flex flex-col sm:flex-row items-center justify-center text-center sm:text-left mb-8 sm:mb-12">
        <span class="text-6xl sm:text-7xl mb-4 sm:mb-0 sm:mr-4">💝</span>
        <div>
          <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold bg-gradient-to-r from-rose-500 to-purple-600 bg-clip-text text-transparent leading-tight">
            Wishare
          </h1>
          <p class="text-base sm:text-lg text-gray-600 mt-2 sm:mt-3">
            <%= t('app.tagline') %>
          </p>
        </div>
      </div>
      
      <!-- Tagline -->
      <h2 class="text-xl sm:text-2xl lg:text-3xl xl:text-4xl font-semibold text-gray-800 mb-4 sm:mb-6 leading-tight px-2">
        <%= t('landing.hero.title') %>
      </h2>
      
      <p class="text-base sm:text-lg lg:text-xl text-gray-600 mb-8 sm:mb-12 max-w-2xl mx-auto px-4 leading-relaxed">
        <%= t('landing.hero.subtitle') %>
      </p>
      
      <!-- Call to Action -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-3 sm:gap-4 mb-12 sm:mb-16 px-4">
        <%= link_to new_user_registration_path, 
              class: "w-full sm:w-auto bg-gradient-to-r from-rose-500 to-rose-600 text-white px-6 sm:px-8 py-3 sm:py-4 rounded-2xl hover:from-rose-600 hover:to-rose-700 focus:outline-none focus:ring-2 focus:ring-rose-500 focus:ring-offset-2 transition duration-200 cursor-pointer font-semibold text-base sm:text-lg touch-manipulation shadow-lg inline-flex items-center justify-center gap-2" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
          </svg>
          <%= t('landing.cta.get_started') %>
        <% end %>
        
        <%= link_to new_user_session_path,
              class: "w-full sm:w-auto border-2 border-rose-500 text-rose-600 px-6 sm:px-8 py-3 sm:py-4 rounded-2xl hover:bg-rose-50 focus:outline-none focus:ring-2 focus:ring-rose-500 focus:ring-offset-2 transition duration-200 cursor-pointer font-semibold text-base sm:text-lg touch-manipulation inline-flex items-center justify-center gap-2" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
          </svg>
          <%= t('landing.cta.sign_in') %>
        <% end %>
      </div>
      
      <!-- Key Highlights -->
      <div class="bg-white/40 backdrop-blur-sm rounded-2xl sm:rounded-3xl p-4 sm:p-8 mb-8 sm:mb-12 border border-rose-100 mx-2">
        <div class="grid grid-cols-2 sm:flex sm:flex-wrap justify-center gap-3 sm:gap-6 text-xs sm:text-sm text-gray-700">
          <div class="flex flex-col sm:flex-row items-center gap-1 sm:gap-2 text-center">
            <span class="text-base sm:text-lg">🔔</span>
            <span class="leading-tight"><%= t('landing.highlights.notifications') %></span>
          </div>
          <div class="flex flex-col sm:flex-row items-center gap-1 sm:gap-2 text-center">
            <span class="text-base sm:text-lg">🌍</span>
            <span class="leading-tight"><%= t('landing.highlights.bilingual') %></span>
          </div>
          <div class="flex flex-col sm:flex-row items-center gap-1 sm:gap-2 text-center">
            <span class="text-base sm:text-lg">📱</span>
            <span class="leading-tight"><%= t('landing.highlights.mobile_ready') %></span>
          </div>
          <div class="flex flex-col sm:flex-row items-center gap-1 sm:gap-2 text-center">
            <span class="text-base sm:text-lg">⚡</span>
            <span class="leading-tight"><%= t('landing.highlights.real_time') %></span>
          </div>
        </div>
      </div>

      <!-- Features -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 max-w-6xl mx-auto mb-8 sm:mb-12 px-2">
        <div class="bg-white/60 backdrop-blur-sm rounded-2xl p-4 sm:p-6 shadow-sm border border-rose-100">
          <div class="text-2xl sm:text-3xl mb-3 sm:mb-4">🎁</div>
          <h3 class="text-base sm:text-lg font-semibold text-gray-800 mb-2 leading-tight">
            <%= t('landing.features.create.title') %>
          </h3>
          <p class="text-gray-600 text-xs sm:text-sm leading-relaxed">
            <%= t('landing.features.create.description') %>
          </p>
        </div>
        
        <div class="bg-white/60 backdrop-blur-sm rounded-2xl p-4 sm:p-6 shadow-sm border border-rose-100">
          <div class="text-2xl sm:text-3xl mb-3 sm:mb-4">👨‍👩‍👧‍👦</div>
          <h3 class="text-base sm:text-lg font-semibold text-gray-800 mb-2 leading-tight">
            <%= t('landing.features.connect.title') %>
          </h3>
          <p class="text-gray-600 text-xs sm:text-sm leading-relaxed">
            <%= t('landing.features.connect.description') %>
          </p>
        </div>
        
        <div class="bg-white/60 backdrop-blur-sm rounded-2xl p-4 sm:p-6 shadow-sm border border-rose-100">
          <div class="text-2xl sm:text-3xl mb-3 sm:mb-4">🎉</div>
          <h3 class="text-base sm:text-lg font-semibold text-gray-800 mb-2 leading-tight">
            <%= t('landing.features.occasions.title') %>
          </h3>
          <p class="text-gray-600 text-xs sm:text-sm leading-relaxed">
            <%= t('landing.features.occasions.description') %>
          </p>
        </div>

        <div class="bg-white/60 backdrop-blur-sm rounded-2xl p-4 sm:p-6 shadow-sm border border-rose-100">
          <div class="text-2xl sm:text-3xl mb-3 sm:mb-4">🔔</div>
          <h3 class="text-base sm:text-lg font-semibold text-gray-800 mb-2 leading-tight">
            <%= t('landing.features.notifications.title') %>
          </h3>
          <p class="text-gray-600 text-xs sm:text-sm leading-relaxed">
            <%= t('landing.features.notifications.description') %>
          </p>
        </div>
      </div>

      <!-- Event Types -->
      <div class="bg-gradient-to-r from-rose-500/10 to-purple-500/10 rounded-2xl sm:rounded-3xl p-4 sm:p-8 mb-8 sm:mb-12 border border-rose-200 mx-2">
        <h3 class="text-lg sm:text-xl lg:text-2xl font-semibold text-gray-800 mb-4 sm:mb-6 text-center">
          <%= t('landing.events.title') %>
        </h3>
        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-3 sm:gap-4 text-center">
          <div class="flex flex-col items-center gap-1 sm:gap-2">
            <span class="text-xl sm:text-2xl">🎂</span>
            <span class="text-xs sm:text-sm text-gray-600 leading-tight"><%= t('landing.events.birthday') %></span>
          </div>
          <div class="flex flex-col items-center gap-1 sm:gap-2">
            <span class="text-xl sm:text-2xl">💒</span>
            <span class="text-xs sm:text-sm text-gray-600 leading-tight"><%= t('landing.events.wedding') %></span>
          </div>
          <div class="flex flex-col items-center gap-1 sm:gap-2">
            <span class="text-xl sm:text-2xl">🍼</span>
            <span class="text-xs sm:text-sm text-gray-600 leading-tight"><%= t('landing.events.baby_shower') %></span>
          </div>
          <div class="flex flex-col items-center gap-1 sm:gap-2">
            <span class="text-xl sm:text-2xl">🎄</span>
            <span class="text-xs sm:text-sm text-gray-600 leading-tight"><%= t('landing.events.christmas') %></span>
          </div>
          <div class="flex flex-col items-center gap-1 sm:gap-2">
            <span class="text-xl sm:text-2xl">🎓</span>
            <span class="text-xs sm:text-sm text-gray-600 leading-tight"><%= t('landing.events.graduation') %></span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>