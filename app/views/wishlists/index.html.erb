<% content_for :title, t('wishlists.index.title') %>
<% content_for :description, t('wishlists.index.subtitle') %>
<% content_for :structured_data, structured_data_website %>

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8"
     data-controller="wishlist-delight pull-to-refresh"
     data-wishlist-delight-celebration-ready-value="true"
     style="min-height: calc(100vh - 140px);">
  <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-4 mb-8">
    <div>
      <% if @focus_partner %>
        <h1 class="heading-page text-gray-800 dark:text-gray-100"><%= @partner.name || t('common.user_fallbacks.friend') %>'s Wishlists</h1>
        <p class="text-body text-gray-600 dark:text-gray-400 mt-1"><%= t('wishlists.show.items_they_love') %></p>
      <% else %>
        <div data-wishlist-delight-target="headerContent">
        <h1 class="heading-page text-gray-800 dark:text-gray-100 flex items-center gap-3">
          <span data-wishlist-delight-target="titleEmoji" class="text-4xl animate-bounce">🎁</span>
          <%= t('wishlists.index.your_wishlists') %>
          <span data-wishlist-delight-target="celebrationBadge" class="hidden bg-gradient-to-r from-rose-500 to-pink-500 text-white text-sm px-3 py-1 rounded-full animate-pulse">✨ Amazing!</span>
        </h1>
        <p class="text-body text-gray-600 dark:text-gray-400 mt-1"><%= t('wishlists.index.subtitle') %></p>
      </div>
      <% end %>
    </div>
    <div class="flex gap-3">
      <% unless @focus_partner %>
        <%= link_to new_wishlist_path, class: "btn-primary bg-gradient-to-r from-rose-500 to-rose-600 text-white px-3 sm:px-4 py-2 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 flex items-center gap-2 shadow-lg transform hover:scale-105 hover:shadow-xl", data: { "wishlist-delight-target": "createButton", action: "mouseenter->wishlist-delight#highlightCreateButton mouseleave->wishlist-delight#resetCreateButton" } do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
          </svg>
          <span class="hidden xs:inline"><%= t('wishlists.index.new_wishlist') %></span>
          <span class="xs:hidden"><%= t('common.actions.new') || t('wishlists.index.new_wishlist') %></span>
        <% end %>
      <% end %>
      <% if @focus_partner %>
        <%= link_to wishlists_path, class: "bg-gray-100 text-gray-600 px-3 sm:px-4 py-2 rounded-lg hover:bg-gray-200 transition duration-200 flex items-center gap-2 text-sm sm:text-base" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          <span class="hidden sm:inline"><%= t('common.actions.back_to_my_wishlists') || t('common.actions.back') %></span>
          <span class="sm:hidden"><%= t('common.actions.back') %></span>
        <% end %>
      <% end %>
    </div>
  </div>

  <% if @wishlists.any? || @connected_wishlists.any? || @public_wishlists.any? %>
    <div class="<%= @focus_partner ? '' : 'mt-12' %>" data-controller="wishlist-filter">
      <% unless @focus_partner %>
        <!-- Filter and Sort Section -->
        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
          <!-- Filter Buttons -->
          <div class="flex flex-wrap gap-2">
          <button data-wishlist-filter-target="filterButton" data-filter="my-wishlists" data-action="click->wishlist-filter#filterBy" class="btn-secondary bg-white dark:bg-gray-700 text-gray-600 dark:text-gray-300 border border-gray-200 dark:border-gray-600 hover:bg-gray-50 dark:hover:bg-gray-600 px-4 py-2 rounded-xl transition duration-200 flex items-center gap-2 cursor-pointer">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
            </svg>
            <%= t('wishlists.index.your_wishlists') %> <span class="filter-count">(<%= @wishlists.count %>)</span>
          </button>
          
          <% if @connected_wishlists.any? %>
            <button data-wishlist-filter-target="filterButton" data-filter="friends-family" data-action="click->wishlist-filter#filterBy" class="btn-secondary bg-white dark:bg-gray-700 text-gray-600 dark:text-gray-300 border border-gray-200 dark:border-gray-600 hover:bg-gray-50 dark:hover:bg-gray-600 px-4 py-2 rounded-xl transition duration-200 flex items-center gap-2 cursor-pointer">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
              </svg>
              <%= t('wishlists.index.connected_wishlists') %> <span class="filter-count">(<%= @connected_wishlists.select(&:partner_only?).count %>)</span>
            </button>
          <% end %>
          
          <% if @connected_wishlists.any? || @public_wishlists.any? %>
            <button data-wishlist-filter-target="filterButton" data-filter="public" data-action="click->wishlist-filter#filterBy" class="btn-secondary bg-white dark:bg-gray-700 text-gray-600 dark:text-gray-300 border border-gray-200 dark:border-gray-600 hover:bg-gray-50 dark:hover:bg-gray-600 px-4 py-2 rounded-xl transition duration-200 flex items-center gap-2 cursor-pointer">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <%= t('wishlists.index.public_wishlists') %> <span class="filter-count">(<%= (@connected_wishlists.select(&:publicly_visible?) + @public_wishlists).count %>)</span>
            </button>
          <% end %>
          
          <button data-wishlist-filter-target="filterButton" data-filter="all" data-action="click->wishlist-filter#filterBy" class="btn-secondary bg-white dark:bg-gray-700 text-gray-600 dark:text-gray-300 border border-gray-200 dark:border-gray-600 hover:bg-gray-50 dark:hover:bg-gray-600 px-4 py-2 rounded-xl transition duration-200 flex items-center gap-2 cursor-pointer">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
            </svg>
            <%= t('common.actions.all') || 'All' %> <span class="filter-count">(<%= (@wishlists + @connected_wishlists + @public_wishlists).count %>)</span>
          </button>
          </div>

          <!-- Sort Dropdown -->
          <div class="relative" data-controller="dropdown">
            <button data-action="click->dropdown#toggle" class="bg-white dark:bg-gray-700 text-gray-600 dark:text-gray-300 border border-gray-200 dark:border-gray-600 hover:bg-gray-50 dark:hover:bg-gray-600 px-4 py-2 rounded-xl transition duration-200 flex items-center gap-2">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4h13M3 8h9m-9 4h6m4 0l4-4m0 0l4 4m-4-4v12"></path>
              </svg>
              <span><%= t('common.sort.sort_by', default: 'Sort by') %></span>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>

            <div data-dropdown-target="menu" class="absolute right-0 mt-2 w-56 bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-200 dark:border-gray-700 py-2 hidden z-50">
              <%= link_to wishlists_path(sort_by: 'created_desc'), class: "block px-4 py-2 text-sm #{'bg-rose-50 dark:bg-rose-900/20 text-rose-600 dark:text-rose-400' if @sort_by == 'created_desc'} text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200" do %>
                <div class="flex items-center justify-between">
                  <span><%= t('common.sort.newest_first', default: 'Newest first') %></span>
                  <% if @sort_by == 'created_desc' %>
                    <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                    </svg>
                  <% end %>
                </div>
              <% end %>

              <%= link_to wishlists_path(sort_by: 'created_asc'), class: "block px-4 py-2 text-sm #{'bg-rose-50 dark:bg-rose-900/20 text-rose-600 dark:text-rose-400' if @sort_by == 'created_asc'} text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200" do %>
                <div class="flex items-center justify-between">
                  <span><%= t('common.sort.oldest_first', default: 'Oldest first') %></span>
                  <% if @sort_by == 'created_asc' %>
                    <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                    </svg>
                  <% end %>
                </div>
              <% end %>

              <%= link_to wishlists_path(sort_by: 'updated_desc'), class: "block px-4 py-2 text-sm #{'bg-rose-50 dark:bg-rose-900/20 text-rose-600 dark:text-rose-400' if @sort_by == 'updated_desc'} text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200" do %>
                <div class="flex items-center justify-between">
                  <span><%= t('common.sort.recently_updated', default: 'Recently updated') %></span>
                  <% if @sort_by == 'updated_desc' %>
                    <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                    </svg>
                  <% end %>
                </div>
              <% end %>

              <%= link_to wishlists_path(sort_by: 'name_asc'), class: "block px-4 py-2 text-sm #{'bg-rose-50 dark:bg-rose-900/20 text-rose-600 dark:text-rose-400' if @sort_by == 'name_asc'} text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200" do %>
                <div class="flex items-center justify-between">
                  <span><%= t('common.sort.name_a_z', default: 'Name (A-Z)') %></span>
                  <% if @sort_by == 'name_asc' %>
                    <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                    </svg>
                  <% end %>
                </div>
              <% end %>

              <%= link_to wishlists_path(sort_by: 'name_desc'), class: "block px-4 py-2 text-sm #{'bg-rose-50 dark:bg-rose-900/20 text-rose-600 dark:text-rose-400' if @sort_by == 'name_desc'} text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200" do %>
                <div class="flex items-center justify-between">
                  <span><%= t('common.sort.name_z_a', default: 'Name (Z-A)') %></span>
                  <% if @sort_by == 'name_desc' %>
                    <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                    </svg>
                  <% end %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
      
      <% if @focus_partner %>
        <!-- Partner focus: Show as grid cards -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <% (@connected_wishlists + @public_wishlists).each do |wishlist| %>
            <div class="bg-white rounded-xl border border-purple-100 hover:border-purple-200 transition duration-200 overflow-hidden">
              <div class="p-6">
                <div class="flex justify-between items-start mb-3">
                  <h3 class="heading-card text-gray-800">
                    <%= link_to wishlist.name, wishlist_path(wishlist), class: "hover:text-purple-600 transition duration-200" %>
                  </h3>
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                  </svg>
                </div>
                
                <% if wishlist.description.present? %>
                  <p class="text-gray-600 text-sm mb-4"><%= truncate(wishlist.description, length: 200) %></p>
                <% end %>
                
                <div class="flex items-center gap-4 text-sm text-gray-500 dark:text-gray-400 mb-4">
                  <div class="flex items-center gap-1">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
                    </svg>
                    <%= t('wishlists.index.item_counts.available', count: wishlist.wishlist_items.available.count) %>
                  </div>
                  <div class="flex items-center gap-1">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <%= t('wishlists.index.item_counts.purchased', count: wishlist.wishlist_items.purchased.count) %>
                  </div>
                </div>
                
                <div class="flex gap-2">
                  <%= link_to t('wishlists.index.buttons.browse_items'), wishlist_path(wishlist), class: "flex-1 text-center bg-purple-600 text-white px-3 py-2 rounded-lg hover:bg-purple-700 transition duration-200 text-sm font-medium" %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <!-- Unified Wishlist Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 sm:gap-6">
          <!-- User's Own Wishlists -->
          <% @wishlists.each do |wishlist| %>
            <div data-wishlist-filter-target="wishlistCard" data-categories="my-wishlists all" class="bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-3xl border border-rose-100 dark:border-gray-700 hover:border-rose-200 dark:hover:border-gray-600 hover:shadow-xl transition duration-300 overflow-hidden group transform hover:scale-[1.02] hover:-translate-y-1"
               data-wishlist-delight-target="wishlistCard"
               data-action="mouseenter->wishlist-delight#onCardHover mouseleave->wishlist-delight#onCardLeave click->wishlist-delight#onCardClick"
               data-wishlist-id="<%= wishlist.id %>"
               data-wishlist-name="<%= wishlist.name %>">
              <!-- Cover Image -->
              <% if wishlist.cover_image.attached? %>
                <div class="h-32 sm:h-40 overflow-hidden">
                  <%= image_tag wishlist.cover_image_url(:card), class: "w-full h-full object-cover", alt: wishlist.name %>
                </div>
              <% end %>
              <div class="p-4 sm:p-6">
                <div class="flex justify-between items-start mb-3">
                  <h3 class="heading-card text-gray-800 dark:text-gray-100 pr-2 group-hover:text-rose-600 dark:group-hover:text-rose-400 transition-colors duration-200">
                    <%= link_to wishlist_path(wishlist), class: "flex items-center gap-2 hover:text-rose-600 dark:hover:text-rose-400 transition duration-200" do %>
                      <span data-wishlist-delight-target="cardEmoji" class="transform group-hover:scale-110 transition-transform duration-200">🎁</span>
                      <span><%= wishlist.name %></span>
                    <% end %>
                  </h3>
                  <div class="flex items-center gap-2 flex-shrink-0">
                    <% if wishlist.is_default %>
                      <span class="bg-gradient-to-r from-rose-100 to-rose-200 text-rose-700 text-xs px-2 py-1 rounded-full font-medium"><%= t('wishlists.index.default_badge') %></span>
                    <% end %>
                    
                    <!-- Three dots menu -->
                    <div class="relative" data-controller="dropdown">
                      <button data-action="click->dropdown#toggle" class="text-gray-400 dark:text-gray-500 hover:text-gray-600 dark:hover:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 p-1 rounded-lg transition duration-200 cursor-pointer">
                        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                          <path d="M10 6a2 2 0 110-4 2 2 0 010 4zM10 12a2 2 0 110-4 2 2 0 010 4zM10 18a2 2 0 110-4 2 2 0 010 4z"></path>
                        </svg>
                      </button>
                      
                      <!-- Dropdown Menu -->
                      <div data-dropdown-target="menu" class="absolute right-0 mt-1 w-48 bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-rose-100 dark:border-gray-700 py-2 hidden z-50">
                        <%= link_to edit_wishlist_path(wishlist), class: "block px-4 py-2 text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200" do %>
                          <div class="flex items-center gap-2">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                            </svg>
                            <%= t('wishlists.show.edit') %> <%= t('navigation.wishlists').singularize || 'Lista' %>
                          </div>
                        <% end %>
                        <%= link_to wishlist, data: { "turbo-method": "delete", confirm: t('wishlists.index.delete_confirmation', name: wishlist.name) }, class: "block px-4 py-2 text-sm text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 transition duration-200 cursor-pointer" do %>
                          <div class="flex items-center gap-2">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                            </svg>
                            <%= t('wishlists.show.delete') %> <%= t('navigation.wishlists').singularize || 'Lista' %>
                          </div>
                        <% end %>
                      </div>
                    </div>
                  </div>
                </div>
                
                <% if wishlist.description.present? %>
                  <p class="text-gray-600 dark:text-gray-400 text-sm mb-4"><%= truncate(wishlist.description, length: 180) %></p>
                <% end %>
                
                <!-- Event Type and Countdown -->
                <% unless wishlist.general_wishlist? %>
                  <div class="bg-gradient-to-r from-amber-50 to-orange-50 dark:from-amber-900/20 dark:to-orange-900/20 rounded-xl p-3 mb-4 border border-amber-200 dark:border-amber-800">
                    <div class="flex items-center justify-between">
                      <div class="flex items-center gap-2">
                        <% if wishlist.event_type == 'birthday' %>
                          <span class="text-lg">🎂</span>
                        <% elsif wishlist.event_type == 'wedding' %>
                          <span class="text-lg">💒</span>
                        <% elsif wishlist.event_type == 'baby_shower' %>
                          <span class="text-lg">👶</span>
                        <% elsif wishlist.event_type == 'christmas' %>
                          <span class="text-lg">🎄</span>
                        <% elsif wishlist.event_type == 'anniversary' %>
                          <span class="text-lg">💕</span>
                        <% elsif wishlist.event_type == 'graduation' %>
                          <span class="text-lg">🎓</span>
                        <% elsif wishlist.event_type == 'housewarming' %>
                          <span class="text-lg">🏡</span>
                        <% elsif wishlist.event_type == 'valentines' %>
                          <span class="text-lg">💝</span>
                        <% elsif wishlist.event_type == 'mothers_day' %>
                          <span class="text-lg">🌸</span>
                        <% elsif wishlist.event_type == 'fathers_day' %>
                          <span class="text-lg">👔</span>
                        <% else %>
                          <span class="text-lg">🎉</span>
                        <% end %>
                        <span class="text-sm font-medium text-amber-700 dark:text-amber-400"><%= wishlist.event_type_display %></span>
                      </div>
                      <% if wishlist.event_date %>
                        <div class="text-right">
                          <% days_until = wishlist.days_until_event %>
                          <% if days_until && days_until >= 0 %>
                            <% if days_until == 0 %>
                              <span class="text-xs font-bold text-rose-600"><%= t('common.time.today') %></span>
                            <% elsif days_until <= 7 %>
                              <span class="text-xs font-bold text-orange-600"><%= t('wishlists.index.days_left', count: days_until) %></span>
                            <% elsif days_until <= 30 %>
                              <span class="text-xs font-semibold text-amber-600"><%= t('wishlists.index.days_left', count: days_until) %></span>
                            <% else %>
                              <span class="text-xs text-gray-600 dark:text-gray-400"><%= wishlist.event_date.strftime("%b %d, %Y") %></span>
                            <% end %>
                          <% elsif wishlist.event_passed? %>
                            <span class="text-xs text-gray-500 dark:text-gray-400"><%= t('wishlists.index.event_passed') %></span>
                          <% else %>
                            <span class="text-xs text-gray-600"><%= wishlist.event_date.strftime("%b %d, %Y") %></span>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  </div>
                <% end %>

                <!-- Item Preview Cards -->
                <% recent_items = wishlist.wishlist_items.order(created_at: :desc).limit(3) %>
                <% if recent_items.any? %>
                  <div class="flex gap-2 mb-4 overflow-x-auto">
                    <% recent_items.each do |item| %>
                      <%= link_to wishlist_wishlist_item_path(wishlist, item), class: "flex-shrink-0 w-20 h-20 bg-gray-100 dark:bg-gray-700 rounded-lg overflow-hidden relative group hover:scale-105 transition-transform duration-200" do %>
                        <% if item.image_url.present? %>
                          <%= image_tag item.image_url, class: "w-full h-full object-cover", alt: item.name %>
                        <% else %>
                          <div class="w-full h-full flex items-center justify-center">
                            <svg class="w-8 h-8 text-gray-400 dark:text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"></path>
                            </svg>
                          </div>
                        <% end %>
                        <% if item.purchased? %>
                          <div class="absolute inset-0 bg-black/50 flex items-center justify-center">
                            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                          </div>
                        <% end %>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-200 flex items-end p-1">
                          <span class="text-xs text-white truncate"><%= item.name %></span>
                        </div>
                      <% end %>
                    <% end %>
                    <% if wishlist.wishlist_items.count > 3 %>
                      <div class="flex-shrink-0 w-20 h-20 bg-gray-100 dark:bg-gray-700 rounded-lg flex items-center justify-center">
                        <span class="text-sm text-gray-600 dark:text-gray-400 font-medium">+<%= wishlist.wishlist_items.count - 3 %></span>
                      </div>
                    <% end %>
                  </div>
                <% end %>

                <div class="flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-4 text-sm text-gray-500 dark:text-gray-400 mb-4">
                  <div class="flex items-center gap-1">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
                    </svg>
                    <%= t('wishlists.index.items_count', count: wishlist.wishlist_items.count) %>
                  </div>
                  <div class="flex items-center gap-1">
                    <% if wishlist.publicly_visible? %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
                      <span class="text-xs text-green-600 font-medium"><%= t('wishlists.index.visibility.publicly_visible') %></span>
                    <% elsif wishlist.partner_only? %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
                      </svg>
                      <span class="text-xs text-blue-600 font-medium"><%= t('wishlists.index.visibility.friends_and_family') %></span>
                    <% else %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                      </svg>
                      <span class="text-xs text-gray-600 font-medium"><%= t('wishlists.index.visibility.private') %></span>
                    <% end %>
                  </div>
                </div>
                
                <div class="flex flex-col sm:flex-row gap-2">
                  <%= link_to t('wishlists.index.buttons.view_items'), wishlist_path(wishlist), class: "flex-1 text-center bg-gradient-to-r from-rose-500 to-rose-600 text-white px-3 py-2 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 text-sm font-medium shadow-sm transform hover:scale-105 hover:shadow-lg", data: { action: "click->wishlist-delight#trackView" } %>
                  <%= link_to new_wishlist_wishlist_item_path(wishlist), class: "text-center bg-amber-50 text-amber-700 px-3 py-2 rounded-xl hover:bg-amber-100 transition duration-200 text-sm font-medium border border-amber-200 transform hover:scale-105 hover:shadow-md flex items-center justify-center gap-2", data: { action: "click->wishlist-delight#trackAddItem" } do %>
                    <span class="transform group-hover:rotate-12 transition-transform duration-200">✨</span>
                    <%= t('wishlists.index.buttons.add_item') %>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
          
          <!-- Friends & Family Wishlists (Partner Only) -->
          <% @connected_wishlists.select(&:partner_only?).each do |wishlist| %>
            <div data-wishlist-filter-target="wishlistCard" data-categories="friends-family all" class="bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-3xl border border-purple-100 dark:border-purple-900/50 hover:border-purple-200 dark:hover:border-purple-800 hover:shadow-xl transition duration-300 overflow-hidden">
              <!-- Cover Image -->
              <% if wishlist.cover_image.attached? %>
                <div class="h-32 sm:h-40 overflow-hidden">
                  <%= image_tag wishlist.cover_image_url(:card), class: "w-full h-full object-cover", alt: wishlist.name %>
                </div>
              <% end %>
              <div class="p-4 sm:p-6">
                <div class="flex justify-between items-start mb-3">
                  <div>
                    <h3 class="heading-card text-gray-800 dark:text-gray-100">
                      <%= link_to wishlist.name, wishlist_path(wishlist), class: "hover:text-purple-600 dark:hover:text-purple-400 transition duration-200" %>
                    </h3>
                    <p class="text-body-sm text-gray-500 dark:text-gray-400 mt-1">by <%= link_to wishlist.user.display_name, user_path(wishlist.user), class: "text-purple-600 dark:text-purple-400 hover:text-purple-800 dark:hover:text-purple-300 transition duration-200 font-medium" %></p>
                  </div>
                  <svg class="w-4 h-4 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                  </svg>
                </div>
                
                <% if wishlist.description.present? %>
                  <p class="text-gray-600 dark:text-gray-400 text-sm mb-4"><%= truncate(wishlist.description, length: 180) %></p>
                <% end %>
                
                <!-- Event Type and Countdown for Friends' Lists -->
                <% unless wishlist.general_wishlist? %>
                  <div class="bg-gradient-to-r from-purple-50 to-pink-50 dark:from-purple-900/20 dark:to-pink-900/20 rounded-xl p-3 mb-4 border border-purple-200 dark:border-purple-800">
                    <div class="flex items-center justify-between">
                      <div class="flex items-center gap-2">
                        <% if wishlist.event_type == 'birthday' %>
                          <span class="text-lg">🎂</span>
                        <% elsif wishlist.event_type == 'wedding' %>
                          <span class="text-lg">💒</span>
                        <% elsif wishlist.event_type == 'baby_shower' %>
                          <span class="text-lg">👶</span>
                        <% elsif wishlist.event_type == 'christmas' %>
                          <span class="text-lg">🎄</span>
                        <% elsif wishlist.event_type == 'anniversary' %>
                          <span class="text-lg">💕</span>
                        <% elsif wishlist.event_type == 'graduation' %>
                          <span class="text-lg">🎓</span>
                        <% elsif wishlist.event_type == 'housewarming' %>
                          <span class="text-lg">🏡</span>
                        <% elsif wishlist.event_type == 'valentines' %>
                          <span class="text-lg">💝</span>
                        <% elsif wishlist.event_type == 'mothers_day' %>
                          <span class="text-lg">🌸</span>
                        <% elsif wishlist.event_type == 'fathers_day' %>
                          <span class="text-lg">👔</span>
                        <% else %>
                          <span class="text-lg">🎉</span>
                        <% end %>
                        <span class="text-sm font-medium text-purple-700 dark:text-purple-400"><%= wishlist.event_type_display %></span>
                      </div>
                      <% if wishlist.event_date %>
                        <div class="text-right">
                          <% days_until = wishlist.days_until_event %>
                          <% if days_until && days_until >= 0 %>
                            <% if days_until == 0 %>
                              <span class="text-xs font-bold text-rose-600"><%= t('common.time.today') %></span>
                            <% elsif days_until <= 7 %>
                              <span class="text-xs font-bold text-orange-600"><%= t('wishlists.index.days_left', count: days_until) %></span>
                            <% elsif days_until <= 30 %>
                              <span class="text-xs font-semibold text-amber-600"><%= t('wishlists.index.days_left', count: days_until) %></span>
                            <% else %>
                              <span class="text-xs text-gray-600 dark:text-gray-400"><%= wishlist.event_date.strftime("%b %d, %Y") %></span>
                            <% end %>
                          <% elsif wishlist.event_passed? %>
                            <span class="text-xs text-gray-500 dark:text-gray-400"><%= t('wishlists.index.event_passed') %></span>
                          <% else %>
                            <span class="text-xs text-gray-600"><%= wishlist.event_date.strftime("%b %d, %Y") %></span>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  </div>
                <% end %>

                <!-- Item Preview Cards -->
                <% recent_items = wishlist.wishlist_items.order(created_at: :desc).limit(3) %>
                <% if recent_items.any? %>
                  <div class="flex gap-2 mb-4 overflow-x-auto">
                    <% recent_items.each do |item| %>
                      <%= link_to wishlist_wishlist_item_path(wishlist, item), class: "flex-shrink-0 w-20 h-20 bg-gray-100 dark:bg-gray-700 rounded-lg overflow-hidden relative group hover:scale-105 transition-transform duration-200" do %>
                        <% if item.image_url.present? %>
                          <%= image_tag item.image_url, class: "w-full h-full object-cover", alt: item.name %>
                        <% else %>
                          <div class="w-full h-full flex items-center justify-center">
                            <svg class="w-8 h-8 text-gray-400 dark:text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"></path>
                            </svg>
                          </div>
                        <% end %>
                        <% if item.purchased? %>
                          <div class="absolute inset-0 bg-black/50 flex items-center justify-center">
                            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                          </div>
                        <% end %>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-200 flex items-end p-1">
                          <span class="text-xs text-white truncate"><%= item.name %></span>
                        </div>
                      <% end %>
                    <% end %>
                    <% if wishlist.wishlist_items.count > 3 %>
                      <div class="flex-shrink-0 w-20 h-20 bg-gray-100 dark:bg-gray-700 rounded-lg flex items-center justify-center">
                        <span class="text-sm text-gray-600 dark:text-gray-400 font-medium">+<%= wishlist.wishlist_items.count - 3 %></span>
                      </div>
                    <% end %>
                  </div>
                <% end %>

                <div class="flex items-center gap-4 text-sm text-gray-500 dark:text-gray-400 mb-4">
                  <div class="flex items-center gap-1">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
                    </svg>
                    <%= t('wishlists.index.items_count', count: wishlist.wishlist_items.count) %>
                  </div>
                </div>
                
                <%= link_to t('wishlists.index.buttons.view_wishlist'), wishlist_path(wishlist), class: "block text-center bg-gradient-to-r from-purple-500 to-purple-600 text-white px-4 py-2 rounded-xl hover:from-purple-600 hover:to-purple-700 transition duration-200 font-medium" %>
              </div>
            </div>
          <% end %>
          
          <!-- Public Wishlists -->
          <% (@connected_wishlists.select(&:publicly_visible?) + @public_wishlists).each do |wishlist| %>
            <div data-wishlist-filter-target="wishlistCard" data-categories="public all" class="bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-3xl border border-green-100 dark:border-green-900/50 hover:border-green-200 dark:hover:border-green-800 hover:shadow-xl transition duration-300 overflow-hidden">
              <!-- Cover Image -->
              <% if wishlist.cover_image.attached? %>
                <div class="h-32 sm:h-40 overflow-hidden">
                  <%= image_tag wishlist.cover_image_url(:card), class: "w-full h-full object-cover", alt: wishlist.name %>
                </div>
              <% end %>
              <div class="p-4 sm:p-6">
                <div class="flex justify-between items-start mb-3">
                  <div>
                    <h3 class="heading-card text-gray-800 dark:text-gray-100">
                      <%= link_to wishlist.name, wishlist_path(wishlist), class: "hover:text-green-600 dark:hover:text-green-400 transition duration-200" %>
                    </h3>
                    <p class="text-body-sm text-gray-500 dark:text-gray-400 mt-1">by <%= link_to wishlist.user.display_name, user_path(wishlist.user), class: "text-green-600 dark:text-green-400 hover:text-green-800 dark:hover:text-green-300 transition duration-200 font-medium" %></p>
                  </div>
                  <span class="bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-400 text-xs px-2 py-1 rounded-full font-medium"><%= t('wishlists.index.visibility.publicly_visible') %></span>
                </div>
                
                <% if wishlist.description.present? %>
                  <p class="text-gray-600 dark:text-gray-400 text-sm mb-4"><%= truncate(wishlist.description, length: 180) %></p>
                <% end %>
                
                <!-- Event Type and Countdown for Public Lists -->
                <% unless wishlist.general_wishlist? %>
                  <div class="bg-gradient-to-r from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 rounded-xl p-3 mb-4 border border-green-200 dark:border-green-800">
                    <div class="flex items-center justify-between">
                      <div class="flex items-center gap-2">
                        <% if wishlist.event_type == 'birthday' %>
                          <span class="text-lg">🎂</span>
                        <% elsif wishlist.event_type == 'wedding' %>
                          <span class="text-lg">💒</span>
                        <% elsif wishlist.event_type == 'baby_shower' %>
                          <span class="text-lg">👶</span>
                        <% elsif wishlist.event_type == 'christmas' %>
                          <span class="text-lg">🎄</span>
                        <% elsif wishlist.event_type == 'anniversary' %>
                          <span class="text-lg">💕</span>
                        <% elsif wishlist.event_type == 'graduation' %>
                          <span class="text-lg">🎓</span>
                        <% elsif wishlist.event_type == 'housewarming' %>
                          <span class="text-lg">🏡</span>
                        <% elsif wishlist.event_type == 'valentines' %>
                          <span class="text-lg">💝</span>
                        <% elsif wishlist.event_type == 'mothers_day' %>
                          <span class="text-lg">🌸</span>
                        <% elsif wishlist.event_type == 'fathers_day' %>
                          <span class="text-lg">👔</span>
                        <% else %>
                          <span class="text-lg">🎉</span>
                        <% end %>
                        <span class="text-sm font-medium text-green-700 dark:text-green-400"><%= wishlist.event_type_display %></span>
                      </div>
                      <% if wishlist.event_date %>
                        <div class="text-right">
                          <% days_until = wishlist.days_until_event %>
                          <% if days_until && days_until >= 0 %>
                            <% if days_until == 0 %>
                              <span class="text-xs font-bold text-rose-600"><%= t('common.time.today') %></span>
                            <% elsif days_until <= 7 %>
                              <span class="text-xs font-bold text-orange-600"><%= t('wishlists.index.days_left', count: days_until) %></span>
                            <% elsif days_until <= 30 %>
                              <span class="text-xs font-semibold text-amber-600"><%= t('wishlists.index.days_left', count: days_until) %></span>
                            <% else %>
                              <span class="text-xs text-gray-600 dark:text-gray-400"><%= wishlist.event_date.strftime("%b %d, %Y") %></span>
                            <% end %>
                          <% elsif wishlist.event_passed? %>
                            <span class="text-xs text-gray-500 dark:text-gray-400"><%= t('wishlists.index.event_passed') %></span>
                          <% else %>
                            <span class="text-xs text-gray-600"><%= wishlist.event_date.strftime("%b %d, %Y") %></span>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  </div>
                <% end %>

                <!-- Item Preview Cards -->
                <% recent_items = wishlist.wishlist_items.order(created_at: :desc).limit(3) %>
                <% if recent_items.any? %>
                  <div class="flex gap-2 mb-4 overflow-x-auto">
                    <% recent_items.each do |item| %>
                      <%= link_to wishlist_wishlist_item_path(wishlist, item), class: "flex-shrink-0 w-20 h-20 bg-gray-100 dark:bg-gray-700 rounded-lg overflow-hidden relative group hover:scale-105 transition-transform duration-200" do %>
                        <% if item.image_url.present? %>
                          <%= image_tag item.image_url, class: "w-full h-full object-cover", alt: item.name %>
                        <% else %>
                          <div class="w-full h-full flex items-center justify-center">
                            <svg class="w-8 h-8 text-gray-400 dark:text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"></path>
                            </svg>
                          </div>
                        <% end %>
                        <% if item.purchased? %>
                          <div class="absolute inset-0 bg-black/50 flex items-center justify-center">
                            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                          </div>
                        <% end %>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-200 flex items-end p-1">
                          <span class="text-xs text-white truncate"><%= item.name %></span>
                        </div>
                      <% end %>
                    <% end %>
                    <% if wishlist.wishlist_items.count > 3 %>
                      <div class="flex-shrink-0 w-20 h-20 bg-gray-100 dark:bg-gray-700 rounded-lg flex items-center justify-center">
                        <span class="text-sm text-gray-600 dark:text-gray-400 font-medium">+<%= wishlist.wishlist_items.count - 3 %></span>
                      </div>
                    <% end %>
                  </div>
                <% end %>

                <div class="flex items-center gap-4 text-sm text-gray-500 dark:text-gray-400 mb-4">
                  <div class="flex items-center gap-1">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
                    </svg>
                    <%= t('wishlists.index.items_count', count: wishlist.wishlist_items.count) %>
                  </div>
                </div>
                
                <%= link_to t('wishlists.index.buttons.view_wishlist'), wishlist_path(wishlist), class: "block text-center bg-gradient-to-r from-green-500 to-green-600 text-white px-4 py-2 rounded-xl hover:from-green-600 hover:to-green-700 transition duration-200 font-medium" %>
              </div>
            </div>
          <% end %>
        </div>
        
        <!-- Empty States for Each Filter -->
        <div data-wishlist-filter-target="emptyState" data-filter="my-wishlists" class="bg-gradient-to-br from-rose-50 to-amber-50 dark:from-rose-900/20 dark:to-amber-900/20 rounded-3xl border-2 border-dashed border-rose-200 dark:border-rose-800 p-12 text-center hidden">
          <div class="bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-full w-16 h-16 mx-auto mb-4 flex items-center justify-center shadow-lg">
            <svg class="w-8 h-8 text-rose-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
          </div>
          <h3 class="heading-section text-gray-800 dark:text-gray-100 mb-2"><%= t('wishlists.index.empty_state.title') %></h3>
          <p class="text-body text-gray-600 dark:text-gray-400 mb-6"><%= t('wishlists.index.empty_state.subtitle') %></p>
          <%= link_to t('wishlists.index.empty_state.get_started'), new_wishlist_path, class: "bg-gradient-to-r from-rose-500 to-rose-600 text-white px-6 py-3 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 font-medium shadow-lg" %>
        </div>
        
        <div data-wishlist-filter-target="emptyState" data-filter="friends-family" class="bg-gradient-to-br from-purple-50 to-pink-50 dark:from-purple-900/20 dark:to-pink-900/20 rounded-3xl border-2 border-dashed border-purple-200 dark:border-purple-800 p-12 text-center hidden">
          <div class="bg-white dark:bg-gray-800 rounded-full w-16 h-16 mx-auto mb-4 flex items-center justify-center">
            <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
            </svg>
          </div>
          <h3 class="heading-section text-gray-800 dark:text-gray-100 mb-2"><%= t('wishlists.index.no_connections.title') %></h3>
          <p class="text-body text-gray-600 dark:text-gray-400 mb-6"><%= t('wishlists.index.no_connections.subtitle') %></p>
          <%= link_to t('wishlists.index.no_connections.send_invitation'), new_invitation_path, class: "bg-gradient-to-r from-purple-500 to-purple-600 text-white px-6 py-3 rounded-xl hover:from-purple-600 hover:to-purple-700 transition duration-200 font-medium shadow-lg" %>
        </div>
        
        <div data-wishlist-filter-target="emptyState" data-filter="public" class="bg-gradient-to-br from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 rounded-3xl border-2 border-dashed border-green-200 dark:border-green-800 p-12 text-center hidden">
          <div class="bg-white dark:bg-gray-800 rounded-full w-16 h-16 mx-auto mb-4 flex items-center justify-center">
            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <h3 class="heading-section text-gray-800 dark:text-gray-100 mb-2"><%= t('common.empty_states.no_public_wishlists') %></h3>
          <p class="text-body text-gray-600 dark:text-gray-400 mb-6"><%= t('common.empty_states.no_public_wishlists_description') %></p>
        </div>
      <% end %>
    </div>
  <% elsif @focus_partner %>
    <!-- Partner focus but no wishlists -->
    <div class="bg-purple-50 dark:bg-purple-900/20 rounded-xl border-2 border-dashed border-purple-200 dark:border-purple-800 p-12 text-center">
      <div class="bg-white rounded-full w-16 h-16 mx-auto mb-4 flex items-center justify-center">
        <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
        </svg>
      </div>
      <h3 class="text-xl font-semibold text-gray-800 dark:text-gray-100 mb-2"><%= t('wishlists.index.partner_empty.title') %></h3>
      <p class="text-gray-600 dark:text-gray-400 mb-6"><%= t('wishlists.index.partner_empty.subtitle') %></p>
      <%= link_to t('wishlists.index.partner_empty.back_to_mine'), wishlists_path, class: "bg-gradient-to-r from-rose-500 to-rose-600 text-white px-6 py-3 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 font-medium shadow-lg" %>
    </div>
  <% elsif current_user.accepted_connections.empty? %>
    <div class="<%= @focus_partner ? '' : 'mt-12' %> bg-gradient-to-br from-rose-50 to-amber-50 dark:from-rose-900/20 dark:to-amber-900/20 rounded-xl p-8 text-center">
      <div class="bg-white rounded-full w-16 h-16 mx-auto mb-4 flex items-center justify-center">
        <svg class="w-8 h-8 text-rose-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
        </svg>
      </div>
      <h3 class="text-xl font-semibold text-gray-800 mb-2"><%= t('wishlists.index.no_connections.title') %></h3>
      <p class="text-gray-600 mb-6"><%= t('wishlists.index.no_connections.subtitle') %></p>
      <%= link_to t('wishlists.index.no_connections.send_invitation'), new_invitation_path, class: "bg-gradient-to-r from-rose-500 to-rose-600 text-white px-6 py-3 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 font-medium shadow-lg" %>
    </div>
  <% end %>
</div>
