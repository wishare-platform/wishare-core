<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Cupid Gifts" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <% if user_signed_in? %>
      <!-- Navigation -->
      <nav class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50" data-controller="mobile-menu">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <div class="flex items-center gap-2">
              <%= link_to root_path, class: "flex items-center gap-2 hover:opacity-80 transition duration-200" do %>
                <svg class="w-8 h-8 text-teal-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 15.546c-.523 0-1.046-.151-1.5-.454a2.704 2.704 0 01-1.042-.918 2.704 2.704 0 01-.454-1.5V8.546c0-.523.151-1.046.454-1.5.303-.454.631-.813 1.042-.918.453-.303.977-.454 1.5-.454s1.046.151 1.5.454c.411.105.739.464 1.042.918.303.454.454.977.454 1.5v4.128c0 .523-.151 1.046-.454 1.5a2.704 2.704 0 01-1.042.918c-.454.303-.977.454-1.5.454zM12 21a9 9 0 100-18 9 9 0 000 18z"></path>
                </svg>
                <span class="text-lg sm:text-xl font-bold text-gray-800">WishShare</span>
              <% end %>
            </div>
            
            <!-- Mobile Menu Button -->
            <div class="md:hidden flex items-center gap-3">
              <div class="bg-teal-100 rounded-full w-8 h-8 flex items-center justify-center">
                <span class="text-sm font-semibold text-teal-700">
                  <%= current_user.name&.first&.upcase || current_user.email&.first&.upcase %>
                </span>
              </div>
              <button data-action="click->mobile-menu#toggle" class="text-gray-600 hover:text-gray-900 focus:outline-none focus:text-gray-900 p-1">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
              </button>
            </div>
            
            <!-- Desktop Navigation Links -->
            <div class="hidden md:flex items-center gap-6">
              <%= link_to "My Wishlists", wishlists_path, class: "text-gray-600 hover:text-gray-900 font-medium transition duration-200 #{request.path.start_with?('/wishlists') ? 'text-teal-600' : ''}" %>
              <%= link_to "Connections", connections_path, class: "text-gray-600 hover:text-gray-900 font-medium transition duration-200 #{request.path.start_with?('/connections') ? 'text-teal-600' : ''}" %>
            </div>
            
            <!-- Desktop User Menu -->
            <div class="hidden md:flex items-center gap-4">
              <%= link_to new_wishlist_path, class: "bg-teal-600 text-white px-3 py-1.5 rounded-lg hover:bg-teal-700 transition duration-200 text-sm font-medium flex items-center gap-2" do %>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
                New Wishlist
              <% end %>
              
              <div class="relative group">
                <button class="flex items-center gap-2 text-gray-600 hover:text-gray-900 transition duration-200">
                  <div class="bg-teal-100 rounded-full w-8 h-8 flex items-center justify-center">
                    <span class="text-sm font-semibold text-teal-700">
                      <%= current_user.name&.first&.upcase || current_user.email&.first&.upcase %>
                    </span>
                  </div>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
                
                <!-- Dropdown Menu -->
                <div class="absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-200 py-2 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                  <div class="px-4 py-2 border-b border-gray-100">
                    <p class="text-sm font-medium text-gray-800"><%= current_user.name || "User" %></p>
                    <p class="text-xs text-gray-500"><%= current_user.email %></p>
                  </div>
                  <%= button_to "Sign out", destroy_user_session_path, method: :delete, 
                        class: "block w-full text-left px-4 py-2 text-sm text-gray-600 hover:bg-gray-50 transition duration-200 border-0 bg-transparent" %>
                </div>
              </div>
            </div>
            
          </div>
          
          <!-- Mobile Menu -->
          <div data-mobile-menu-target="menu" class="hidden md:hidden pb-4">
            <div class="flex flex-col gap-3">
              <%= link_to "My Wishlists", wishlists_path, class: "text-gray-600 hover:text-gray-900 font-medium transition duration-200 py-2 #{request.path.start_with?('/wishlists') ? 'text-teal-600' : ''}" %>
              <%= link_to "Connections", connections_path, class: "text-gray-600 hover:text-gray-900 font-medium transition duration-200 py-2 #{request.path.start_with?('/connections') ? 'text-teal-600' : ''}" %>
              
              <div class="border-t border-gray-200 pt-3 mt-3">
                <%= link_to new_wishlist_path, class: "bg-teal-600 text-white px-4 py-2 rounded-lg hover:bg-teal-700 transition duration-200 text-sm font-medium flex items-center gap-2 w-full justify-center" do %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                  </svg>
                  New Wishlist
                <% end %>
                
                <div class="mt-3 pt-3 border-t border-gray-200">
                  <div class="mb-3">
                    <p class="text-sm font-medium text-gray-800"><%= current_user.name || "User" %></p>
                    <p class="text-xs text-gray-500"><%= current_user.email %></p>
                  </div>
                  <%= button_to "Sign out", destroy_user_session_path, method: :delete, 
                        class: "block w-full text-left py-2 text-sm text-red-600 hover:text-red-700 transition duration-200 border-0 bg-transparent font-medium" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-xl flex items-start gap-3" role="alert">
          <svg class="w-5 h-5 text-green-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= notice %></span>
        </div>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-xl flex items-start gap-3" role="alert">
          <svg class="w-5 h-5 text-red-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= alert %></span>
        </div>
      </div>
    <% end %>
    
    <main class="<%= user_signed_in? ? 'pt-4' : 'pt-8' %>">
      <%= yield %>
    </main>
  </body>
</html>
