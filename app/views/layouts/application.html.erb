<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Wishare" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-br from-rose-50 via-amber-50 to-purple-50 min-h-screen">
    <% if user_signed_in? %>
      <!-- Navigation -->
      <nav class="bg-white/95 backdrop-blur-md shadow-sm border-b border-rose-100 sticky top-0 z-50" data-controller="mobile-menu">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <div class="flex items-center gap-2">
              <%= link_to root_path, class: "flex items-center gap-2 hover:opacity-80 transition duration-200" do %>
                <span class="text-2xl">💝</span>
                <span class="text-lg sm:text-xl font-bold bg-gradient-to-r from-rose-500 to-purple-600 bg-clip-text text-transparent">WishShare</span>
              <% end %>
            </div>
            
            <!-- Mobile Menu Button -->
            <div class="md:hidden flex items-center gap-3">
              <% if current_user.avatar_url.present? %>
                <img src="<%= current_user.avatar_url %>" alt="Avatar" class="w-8 h-8 rounded-full border-2 border-rose-200">
              <% else %>
                <div class="bg-gradient-to-r from-rose-100 to-purple-100 rounded-full w-8 h-8 flex items-center justify-center">
                  <span class="text-sm font-semibold text-rose-700">
                    <%= current_user.name&.first&.upcase || current_user.email&.first&.upcase %>
                  </span>
                </div>
              <% end %>
              <button data-action="click->mobile-menu#toggle" class="text-gray-600 hover:text-gray-900 focus:outline-none focus:text-gray-900 p-1">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
              </button>
            </div>
            
            <!-- Desktop Navigation Links -->
            <div class="hidden md:flex items-center gap-6">
              <%= link_to "My Wishlists", wishlists_path, class: "text-gray-600 hover:text-rose-600 font-medium transition duration-200 #{request.path.start_with?('/wishlists') ? 'text-rose-600' : ''}" %>
              <%= link_to "Connections", connections_path, class: "text-gray-600 hover:text-rose-600 font-medium transition duration-200 #{request.path.start_with?('/connections') ? 'text-rose-600' : ''}" %>
            </div>
            
            <!-- Desktop User Menu -->
            <div class="hidden md:flex items-center gap-4">
              <%= link_to new_wishlist_path, class: "bg-gradient-to-r from-rose-500 to-rose-600 text-white px-3 py-1.5 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 text-sm font-medium flex items-center gap-2 shadow-sm" do %>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
                New Wishlist
              <% end %>
              
              <div class="relative group">
                <button class="flex items-center gap-2 text-gray-600 hover:text-gray-900 transition duration-200">
                  <% if current_user.avatar_url.present? %>
                    <img src="<%= current_user.avatar_url %>" alt="Avatar" class="w-8 h-8 rounded-full border-2 border-rose-200">
                  <% else %>
                    <div class="bg-gradient-to-r from-rose-100 to-purple-100 rounded-full w-8 h-8 flex items-center justify-center">
                      <span class="text-sm font-semibold text-rose-700">
                        <%= current_user.name&.first&.upcase || current_user.email&.first&.upcase %>
                      </span>
                    </div>
                  <% end %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
                
                <!-- Dropdown Menu -->
                <div class="absolute right-0 mt-2 w-48 bg-white rounded-xl shadow-lg border border-rose-100 py-2 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                  <div class="px-4 py-2 border-b border-rose-50">
                    <p class="text-sm font-medium text-gray-800"><%= current_user.name || "User" %></p>
                    <p class="text-xs text-gray-500"><%= current_user.email %></p>
                  </div>
                  <%= link_to edit_user_registration_path, class: "block px-4 py-2 text-sm text-gray-600 hover:bg-gray-50 transition duration-200" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                      </svg>
                      Edit Profile
                    </div>
                  <% end %>
                  <%= button_to "Sign out", destroy_user_session_path, method: :delete, 
                        class: "block w-full text-left px-4 py-2 text-sm text-gray-600 hover:bg-gray-50 transition duration-200 border-0 bg-transparent" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                      </svg>
                      Sign out
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
            
          </div>
          
          <!-- Mobile Menu -->
          <div data-mobile-menu-target="menu" class="hidden md:hidden pb-4">
            <div class="flex flex-col gap-3">
              <%= link_to "My Wishlists", wishlists_path, class: "text-gray-600 hover:text-rose-600 font-medium transition duration-200 py-2 #{request.path.start_with?('/wishlists') ? 'text-rose-600' : ''}" %>
              <%= link_to "Connections", connections_path, class: "text-gray-600 hover:text-rose-600 font-medium transition duration-200 py-2 #{request.path.start_with?('/connections') ? 'text-rose-600' : ''}" %>
              
              <div class="border-t border-rose-100 pt-3 mt-3">
                <%= link_to new_wishlist_path, class: "bg-gradient-to-r from-rose-500 to-rose-600 text-white px-4 py-2 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 text-sm font-medium flex items-center gap-2 w-full justify-center shadow-sm" do %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                  </svg>
                  New Wishlist
                <% end %>
                
                <div class="mt-3 pt-3 border-t border-rose-100">
                  <div class="mb-3">
                    <p class="text-sm font-medium text-gray-800"><%= current_user.name || "User" %></p>
                    <p class="text-xs text-gray-500"><%= current_user.email %></p>
                  </div>
                  <%= button_to "Sign out", destroy_user_session_path, method: :delete, 
                        class: "block w-full text-left py-2 text-sm text-rose-600 hover:text-rose-700 transition duration-200 border-0 bg-transparent font-medium" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <div class="bg-emerald-50 border border-emerald-200 text-emerald-700 px-4 py-3 rounded-xl flex items-start gap-3" role="alert">
          <svg class="w-5 h-5 text-emerald-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= notice %></span>
        </div>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <div class="bg-rose-50 border border-rose-200 text-rose-700 px-4 py-3 rounded-xl flex items-start gap-3" role="alert">
          <svg class="w-5 h-5 text-rose-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= alert %></span>
        </div>
      </div>
    <% end %>
    
    <main class="<%= user_signed_in? ? 'pt-4' : 'pt-8' %>">
      <%= yield %>
    </main>
  </body>
</html>
