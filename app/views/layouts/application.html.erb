<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) ? "#{content_for(:title)} | Wishare" : "Wishare - Share Wishlists with Friends & Family" %></title>
    <meta name="description" content="<%= content_for(:description) || "Create and share wishlists with friends and family. Perfect for birthdays, holidays, and special occasions. Make gifting meaningful with Wishare." %>">
    <meta name="keywords" content="wishlist, gifts, birthday gifts, holiday gifts, gift sharing, family gifts, present ideas, gift registry">
    <meta name="author" content="Wishare">
    <meta name="robots" content="index, follow">
    
    <!-- Viewport and mobile optimization -->
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#EC4899">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:title" content="<%= content_for(:title) ? "#{content_for(:title)} | Wishare" : "Wishare - Share Wishlists with Friends & Family" %>">
    <meta property="og:description" content="<%= content_for(:description) || "Create and share wishlists with friends and family. Perfect for birthdays, holidays, and special occasions. Make gifting meaningful with Wishare." %>">
    <meta property="og:image" content="<%= request.base_url %>/wishare-og-image.png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:site_name" content="Wishare">
    <meta property="og:locale" content="<%= I18n.locale == :'pt-BR' ? 'pt_BR' : 'en_US' %>">
    
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:url" content="<%= request.original_url %>">
    <meta name="twitter:title" content="<%= content_for(:title) ? "#{content_for(:title)} | Wishare" : "Wishare - Share Wishlists with Friends & Family" %>">
    <meta name="twitter:description" content="<%= content_for(:description) || "Create and share wishlists with friends and family. Perfect for birthdays, holidays, and special occasions." %>">
    <meta name="twitter:image" content="<%= request.base_url %>/wishare-og-image.png">
    <meta name="twitter:creator" content="@wishare">
    
    <!-- Additional SEO -->
    <link rel="canonical" href="<%= request.original_url %>">
    <meta name="application-name" content="Wishare">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Structured Data -->
    <% if content_for?(:structured_data) %>
      <script type="application/ld+json">
        <%= content_for(:structured_data).html_safe %>
      </script>
    <% end %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Favicon -->
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="icon" href="/favicon-16x16.svg" type="image/svg+xml" sizes="16x16">
    <link rel="icon" href="/favicon-32x32.svg" type="image/svg+xml" sizes="32x32">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-br from-rose-50 via-amber-50 to-purple-50 min-h-screen">
    <% if user_signed_in? %>
      <!-- Navigation -->
      <nav class="bg-white/95 backdrop-blur-md shadow-sm border-b border-rose-100 sticky top-0 z-50" data-controller="mobile-menu">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <div class="flex items-center gap-2">
              <%= link_to root_path, class: "flex items-center gap-2 hover:opacity-80 transition duration-200" do %>
                <span class="text-2xl">💝</span>
                <span class="text-lg sm:text-xl font-bold bg-gradient-to-r from-rose-500 to-purple-600 bg-clip-text text-transparent">Wishare</span>
              <% end %>
            </div>
            
            <!-- Mobile Menu Button -->
            <div class="md:hidden flex items-center gap-3">
              <% if current_user.avatar_url.present? %>
                <img src="<%= current_user.avatar_url %>" alt="Avatar" class="w-8 h-8 rounded-full border-2 border-rose-200">
              <% else %>
                <div class="bg-gradient-to-r from-rose-100 to-purple-100 rounded-full w-8 h-8 flex items-center justify-center">
                  <span class="text-sm font-semibold text-rose-700">
                    <%= current_user.name&.first&.upcase || current_user.email&.first&.upcase %>
                  </span>
                </div>
              <% end %>
              <button data-action="click->mobile-menu#toggle" class="text-gray-600 hover:text-gray-900 focus:outline-none focus:text-gray-900 p-1">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
              </button>
            </div>
            
            <!-- Desktop Navigation Links -->
            <div class="hidden md:flex items-center gap-6">
              <%= link_to t('navigation.wishlists'), wishlists_path, class: "text-gray-600 hover:text-rose-600 font-medium transition duration-200 #{request.path.match?(/\/wishlists/) ? 'text-rose-600' : ''}" %>
              <%= link_to t('navigation.connections'), connections_path, class: "text-gray-600 hover:text-rose-600 font-medium transition duration-200 #{request.path.match?(/\/connections/) ? 'text-rose-600' : ''}" %>
            </div>
            
            <!-- Desktop User Menu -->
            <div class="hidden md:flex items-center gap-3">
              <%= link_to new_invitation_path, class: "bg-white border border-rose-200 text-rose-600 px-3 py-1.5 rounded-xl hover:bg-rose-50 transition duration-200 text-sm font-medium flex items-center gap-2" do %>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                </svg>
<%= t('navigation.invite_friend') %>
              <% end %>
              
              <%= link_to new_wishlist_path, class: "bg-gradient-to-r from-rose-500 to-rose-600 text-white px-3 py-1.5 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 text-sm font-medium flex items-center gap-2 shadow-sm" do %>
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                </svg>
<%= t('wishlists.index.new_wishlist') %>
              <% end %>
              
              <div class="relative group">
                <button class="flex items-center gap-2 text-gray-600 hover:text-gray-900 transition duration-200">
                  <% if current_user.avatar_url.present? %>
                    <img src="<%= current_user.avatar_url %>" alt="Avatar" class="w-8 h-8 rounded-full border-2 border-rose-200">
                  <% else %>
                    <div class="bg-gradient-to-r from-rose-100 to-purple-100 rounded-full w-8 h-8 flex items-center justify-center">
                      <span class="text-sm font-semibold text-rose-700">
                        <%= current_user.name&.first&.upcase || current_user.email&.first&.upcase %>
                      </span>
                    </div>
                  <% end %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
                
                <!-- Dropdown Menu -->
                <div class="absolute right-0 mt-2 w-48 bg-white rounded-xl shadow-lg border border-rose-100 py-2 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200">
                  <div class="px-4 py-2 border-b border-rose-50">
                    <p class="text-sm font-medium text-gray-800"><%= current_user.name || "User" %></p>
                    <p class="text-xs text-gray-500"><%= current_user.email %></p>
                  </div>
                  <%= link_to edit_user_registration_path, class: "block px-4 py-2 text-sm text-gray-600 hover:bg-gray-50 transition duration-200" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                      </svg>
<%= t('profile.edit.title') %>
                    </div>
                  <% end %>
                  <%= link_to user_path(current_user), class: "block px-4 py-2 text-sm text-gray-600 hover:bg-gray-50 transition duration-200" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
<%= t('navigation.view_public_profile') %>
                    </div>
                  <% end %>
                  
                  <div class="border-t border-rose-50 my-2"></div>
                  
                  <!-- Language Switcher -->
                  <div class="px-4 py-2">
                    <p class="text-xs text-gray-500 mb-2 uppercase tracking-wide">Language</p>
                    <% available_locales.each do |locale| %>
                      <%= link_to locale_switch_url(locale[:code]), 
                            class: "flex items-center gap-2 px-2 py-1.5 text-sm rounded-lg transition duration-200 #{'bg-rose-50 text-rose-700' if I18n.locale.to_s == locale[:code]} #{'text-gray-600 hover:bg-gray-50' unless I18n.locale.to_s == locale[:code]}" do %>
                        <span class="text-base"><%= locale[:flag] %></span>
                        <span><%= locale[:name] %></span>
                        <% if I18n.locale.to_s == locale[:code] %>
                          <svg class="w-3 h-3 ml-auto text-rose-600" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
                          </svg>
                        <% end %>
                      <% end %>
                    <% end %>
                  </div>
                  
                  <div class="border-t border-rose-50"></div>
                  
                  <%= link_to destroy_user_session_path, 
                        data: { "turbo-method": :delete },
                        class: "block w-full text-left px-4 py-2 text-sm text-gray-600 hover:bg-gray-50 transition duration-200" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                      </svg>
<%= t('navigation.sign_out') %>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
            
          </div>
          
          <!-- Mobile Menu -->
          <div data-mobile-menu-target="menu" class="hidden md:hidden pb-4">
            <div class="flex flex-col gap-3">
              <%= link_to t('navigation.wishlists'), wishlists_path, class: "text-gray-600 hover:text-rose-600 font-medium transition duration-200 py-2 #{request.path.match?(/\/wishlists/) ? 'text-rose-600' : ''}" %>
              <%= link_to t('navigation.connections'), connections_path, class: "text-gray-600 hover:text-rose-600 font-medium transition duration-200 py-2 #{request.path.match?(/\/connections/) ? 'text-rose-600' : ''}" %>
              
              <div class="border-t border-rose-100 pt-3 mt-3 flex flex-col gap-2">
                <%= link_to new_invitation_path, class: "bg-white border border-rose-200 text-rose-600 px-4 py-2 rounded-xl hover:bg-rose-50 transition duration-200 text-sm font-medium flex items-center gap-2 w-full justify-center" do %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                  </svg>
  <%= t('navigation.invite_friend') %>
                <% end %>
                
                <%= link_to new_wishlist_path, class: "bg-gradient-to-r from-rose-500 to-rose-600 text-white px-4 py-2 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 text-sm font-medium flex items-center gap-2 w-full justify-center shadow-sm" do %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                  </svg>
  <%= t('wishlists.index.new_wishlist') %>
                <% end %>
                
                <div class="mt-3 pt-3 border-t border-rose-100">
                  <div class="mb-3">
                    <p class="text-sm font-medium text-gray-800"><%= current_user.name || "User" %></p>
                    <p class="text-xs text-gray-500"><%= current_user.email %></p>
                  </div>
                  
                  <!-- Mobile Language Switcher -->
                  <div class="mb-4">
                    <p class="text-xs text-gray-500 mb-2 uppercase tracking-wide">Language</p>
                    <div class="flex gap-2">
                      <% available_locales.each do |locale| %>
                        <%= link_to locale_switch_url(locale[:code]), 
                              class: "flex items-center gap-2 px-3 py-2 rounded-lg text-sm transition duration-200 #{'bg-rose-50 text-rose-700 border border-rose-200' if I18n.locale.to_s == locale[:code]} #{'text-gray-600 hover:bg-gray-50 border border-gray-200' unless I18n.locale.to_s == locale[:code]}" do %>
                          <span><%= locale[:flag] %></span>
                          <span><%= locale[:name] %></span>
                          <% if I18n.locale.to_s == locale[:code] %>
                            <svg class="w-3 h-3 ml-auto text-rose-600" fill="currentColor" viewBox="0 0 24 24">
                              <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
                            </svg>
                          <% end %>
                        <% end %>
                      <% end %>
                    </div>
                  </div>
                  
                  <%= button_to t('navigation.sign_out'), destroy_user_session_path, method: :delete, 
                        class: "block w-full text-left py-2 text-sm text-rose-600 hover:text-rose-700 transition duration-200 border-0 bg-transparent font-medium" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <div class="bg-emerald-50 border border-emerald-200 text-emerald-700 px-4 py-3 rounded-xl flex items-start gap-3" role="alert">
          <svg class="w-5 h-5 text-emerald-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= notice %></span>
        </div>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <div class="bg-rose-50 border border-rose-200 text-rose-700 px-4 py-3 rounded-xl flex items-start gap-3" role="alert">
          <svg class="w-5 h-5 text-rose-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= alert %></span>
        </div>
      </div>
    <% end %>
    
    <main class="<%= user_signed_in? ? 'pt-4' : 'pt-8' %> pb-16">
      <%= yield %>
    </main>
    
    <!-- Footer -->
    <footer class="bg-white/80 backdrop-blur-md border-t border-gray-200 mt-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="flex flex-col md:flex-row justify-between items-center gap-4">
          <!-- Logo and tagline -->
          <div class="flex items-center gap-2 text-center md:text-left">
            <span class="text-xl">💝</span>
            <div>
              <div class="font-bold bg-gradient-to-r from-rose-500 to-purple-600 bg-clip-text text-transparent">
                Wishare
              </div>
              <div class="text-xs text-gray-500"><%= t('common.footer.tagline') %></div>
            </div>
          </div>
          
          <!-- Legal links -->
          <div class="flex flex-wrap items-center justify-center gap-4 text-sm text-gray-600">
            <%= link_to t('common.footer.terms_of_service'), terms_of_service_path, class: "hover:text-rose-600 transition duration-200" %>
            <span class="text-gray-300">•</span>
            <%= link_to t('common.footer.privacy_policy'), privacy_policy_path, class: "hover:text-rose-600 transition duration-200" %>
            <span class="text-gray-300 hidden sm:inline">•</span>
            <span class="text-gray-400 text-xs"><%= t('common.footer.copyright', year: Date.current.year) %></span>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
