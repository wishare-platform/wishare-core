<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) ? "#{content_for(:title)} | Wishare" : "Wishare - Share Wishlists with Friends & Family" %></title>
    <meta name="description" content="<%= content_for(:description) || "Create and share wishlists with friends and family. Perfect for birthdays, holidays, and special occasions. Make gifting meaningful with Wishare." %>">
    <meta name="keywords" content="wishlist, gifts, birthday gifts, holiday gifts, gift sharing, family gifts, present ideas, gift registry">
    <meta name="author" content="Wishare">
    <meta name="robots" content="index, follow">
    
    <!-- Viewport and mobile optimization -->
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#EC4899">
    
    <!-- Open Graph / Facebook -->
    <meta property="fb:app_id" content="">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:title" content="<%= content_for(:title) ? "#{content_for(:title)} | Wishare" : "Wishare - Share Wishlists with Friends & Family" %>">
    <meta property="og:description" content="<%= content_for(:description) || "Create and share wishlists with friends and family. Perfect for birthdays, holidays, and special occasions. Make gifting meaningful with Wishare." %>">
    <meta property="og:image" content="<%= request.base_url %>/wishare-og-image.png?v=<%= Date.current.strftime('%Y%m%d') %>">
    <meta property="og:image:secure_url" content="<%= request.base_url %>/wishare-og-image.png?v=<%= Date.current.strftime('%Y%m%d') %>">
    <meta property="og:image:type" content="image/png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:image:alt" content="Wishare - Thoughtful Gifting Made Simple">
    <meta property="og:site_name" content="Wishare">
    <meta property="og:locale" content="<%= I18n.locale == :'pt-BR' ? 'pt_BR' : 'en_US' %>">
    
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:url" content="<%= request.original_url %>">
    <meta name="twitter:title" content="<%= content_for(:title) ? "#{content_for(:title)} | Wishare" : "Wishare - Share Wishlists with Friends & Family" %>">
    <meta name="twitter:description" content="<%= content_for(:description) || "Create and share wishlists with friends and family. Perfect for birthdays, holidays, and special occasions." %>">
    <meta name="twitter:image" content="<%= request.base_url %>/wishare-og-image.png?v=<%= Date.current.strftime('%Y%m%d') %>">
    <meta name="twitter:creator" content="@wishare">
    
    <!-- Additional SEO -->
    <link rel="canonical" href="<%= request.original_url %>">
    <meta name="application-name" content="Wishare">
    
    <% 
      # Load GTM only if user has explicitly given analytics consent
      consent_record = CookieConsent.find_consent_for_request(request, current_user)
      has_analytics_consent = consent_record&.analytics_enabled? == true
    %>
    
    <!-- Initialize dataLayer regardless of consent -->
    <script>
      window.dataLayer = window.dataLayer || [];
    </script>
    
    <!-- Theme Detection Script - Prevent flash of wrong theme -->
    <script>
      (function() {
        const savedTheme = localStorage.getItem('theme') || '<%= user_signed_in? ? current_user.theme_preference : (session[:theme_preference] || "system") %>';
        if (savedTheme === 'dark' || (savedTheme === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>
    
    <% if has_analytics_consent %>
    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-53FTHDK7');</script>
    <!-- End Google Tag Manager -->
    <% else %>
    <!-- GTM Placeholder - Will load after consent -->
    <script>
      window.gtmId = 'GTM-53FTHDK7';
      window.gtmPendingConsent = true;
    </script>
    <% end %>
    
    <!-- Google Fonts - Playfair Display and Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Structured Data -->
    <% if content_for?(:structured_data) %>
      <script type="application/ld+json">
        <%= content_for(:structured_data).html_safe %>
      </script>
    <% end %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Favicon -->
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-br from-rose-50 via-amber-50 to-purple-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 min-h-screen flex flex-col">
    <% if has_analytics_consent %>
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-53FTHDK7"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->
    <% end %>
    
    <% if user_signed_in? %>
      <!-- Navigation -->
      <nav class="bg-white/95 dark:bg-gray-800/95 backdrop-blur-md shadow-sm border-b border-rose-100 dark:border-gray-700 sticky top-0 z-50 w-full" data-controller="mobile-menu">
        <div class="w-full px-4 sm:px-6 lg:px-8">
          <div class="flex items-center justify-between h-16">
            <!-- Logo -->
            <div class="flex items-center gap-2 flex-shrink-0">
              <%= link_to root_path, class: "flex items-center gap-2 hover:opacity-80 transition duration-200" do %>
                <span class="text-2xl">💝</span>
                <span class="text-accent-lg bg-gradient-to-r from-rose-500 to-purple-600 bg-clip-text text-transparent">Wishare</span>
              <% end %>
            </div>
            
            <!-- Right Actions & User Menu -->
            <div class="flex items-center gap-2 flex-shrink-0">
              <!-- Desktop Quick Actions -->
              <div class="hidden md:flex items-center gap-2">
                <!-- Notification Bell -->
                <div class="relative" data-controller="notifications" data-notifications-user-id-value="<%= current_user.id %>">
                  <button data-action="click->notifications#toggle" class="relative p-2 text-gray-600 dark:text-gray-300 hover:text-rose-600 dark:hover:text-rose-400 transition duration-200 rounded-lg hover:bg-rose-50 dark:hover:bg-gray-700 cursor-pointer">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path>
                    </svg>
                    <% if current_user.unread_notifications_count > 0 %>
                      <span data-notifications-target="badge" class="absolute -top-1 -right-1 bg-rose-500 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center font-semibold">
                        <%= current_user.unread_notifications_count %>
                      </span>
                    <% else %>
                      <span data-notifications-target="badge" class="hidden absolute -top-1 -right-1 bg-rose-500 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center font-semibold">
                        0
                      </span>
                    <% end %>
                  </button>
                
                  <!-- Notification Dropdown -->
                  <div data-notifications-target="dropdown" class="hidden absolute right-0 mt-2 w-80 bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-rose-100 dark:border-gray-700 z-50">
                    <div class="px-4 py-3 border-b border-rose-50 dark:border-gray-700 flex items-center justify-between">
                      <h3 class="heading-sub text-gray-800 dark:text-gray-200"><%= t('notifications.bell_title') %></h3>
                      <% if current_user.unread_notifications_count > 0 %>
                        <%= link_to t('notifications.mark_all_read'), mark_all_as_read_notifications_path, 
                              data: { "turbo-method": :patch },
                              class: "text-xs text-rose-600 dark:text-rose-400 hover:text-rose-700 dark:hover:text-rose-300" %>
                      <% end %>
                    </div>
                    
                    <div data-notifications-target="list" class="max-h-96 overflow-y-auto">
                      <% if current_user.notifications.recent.limit(10).any? %>
                        <% current_user.notifications.recent.limit(10).each do |notification| %>
                          <div class="border-b border-gray-100 dark:border-gray-700 <%= 'bg-rose-50 dark:bg-rose-900/10' unless notification.read? %>">
                            <% unless notification.read? %>
                              <%= link_to mark_as_read_notification_path(notification), 
                                    data: { "turbo-method": :patch },
                                    class: "block px-4 py-3 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition duration-200" do %>
                                <div class="flex items-start gap-3">
                                  <div class="flex-shrink-0 w-2 h-2 mt-2 rounded-full bg-rose-500 dark:bg-rose-400"></div>
                                  <div class="flex-1">
                                    <p class="text-sm font-medium text-gray-800 dark:text-gray-100"><%= notification.localized_title %></p>
                                    <p class="text-xs text-gray-600 dark:text-gray-300 mt-1"><%= notification.localized_message %></p>
                                    <p class="text-xs text-gray-400 dark:text-gray-500 mt-1"><%= time_ago_in_words(notification.created_at) %> ago</p>
                                  </div>
                                </div>
                              <% end %>
                              
                              <% if notification.notification_type == 'invitation_received' && notification.data && notification.data['invitation_token'] %>
                                <% invitation = Invitation.find_by(token: notification.data['invitation_token']) %>
                                <% if invitation && invitation.pending? %>
                                  <div class="px-4 pb-3 flex items-center justify-end gap-1">
                                    <%= link_to accept_invitation_path(token: notification.data['invitation_token']), 
                                          data: { "turbo-method": :patch },
                                          class: "p-1.5 bg-green-100 dark:bg-green-900/30 hover:bg-green-200 dark:hover:bg-green-800/50 rounded-lg transition duration-200",
                                          title: t('notifications.actions.accept') do %>
                                      <svg class="w-4 h-4 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                                      </svg>
                                    <% end %>
                                    <%= link_to accept_invitation_path(token: notification.data['invitation_token'], accept: false), 
                                          data: { "turbo-method": :patch },
                                          class: "p-1.5 bg-red-100 dark:bg-red-900/30 hover:bg-red-200 dark:hover:bg-red-800/50 rounded-lg transition duration-200",
                                          title: t('notifications.actions.decline') do %>
                                      <svg class="w-4 h-4 text-red-600 dark:text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                                      </svg>
                                    <% end %>
                                  </div>
                                <% end %>
                              <% end %>
                            <% else %>
                              <div class="px-4 py-3">
                                <div class="flex items-start gap-3">
                                  <div class="flex-shrink-0 w-2 h-2 mt-2 rounded-full bg-gray-300 dark:bg-gray-600"></div>
                                  <div class="flex-1">
                                    <p class="text-sm font-medium text-gray-800 dark:text-gray-100"><%= notification.localized_title %></p>
                                    <p class="text-xs text-gray-600 dark:text-gray-300 mt-1"><%= notification.localized_message %></p>
                                    <p class="text-xs text-gray-400 dark:text-gray-500 mt-1"><%= time_ago_in_words(notification.created_at) %> ago</p>
                                  </div>
                                </div>
                              </div>
                            <% end %>
                          </div>
                        <% end %>
                      <% else %>
                        <div class="px-4 py-8 text-center text-gray-500 dark:text-gray-400">
                          <svg class="w-12 h-12 mx-auto mb-3 text-gray-300 dark:text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"></path>
                          </svg>
                          <p class="text-sm"><%= t('notifications.empty_state') %></p>
                        </div>
                      <% end %>
                    </div>
                    
                    <div class="px-4 py-2 border-t border-rose-50 dark:border-gray-700">
                      <%= link_to t('notifications.view_all'), notifications_path, 
                            class: "text-sm text-rose-600 dark:text-rose-400 hover:text-rose-700 dark:hover:text-rose-300 font-medium" %>
                    </div>
                  </div>
                </div>
                
                <!-- Quick Action Buttons -->
                <div class="flex items-center gap-1 border-l border-gray-200 pl-2 ml-2">
                  <!-- Add Item Button -->
                  <div class="relative" data-controller="dropdown">
                    <button data-action="click->dropdown#toggle" class="p-2 bg-gradient-to-r from-rose-100 to-purple-100 dark:from-rose-900/50 dark:to-purple-900/50 text-rose-600 dark:text-rose-400 hover:from-rose-500 hover:to-rose-600 dark:hover:from-rose-600 dark:hover:to-rose-700 hover:text-white dark:hover:text-white transition-all duration-200 rounded-xl shadow-sm hover:shadow-md cursor-pointer" title="<%= t('items.new') %>">
                      <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                      </svg>
                    </button>
                    
                    <div data-dropdown-target="menu" class="absolute right-0 mt-2 w-64 bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-rose-100 dark:border-gray-700 py-2 hidden z-50">
                      <div class="px-4 py-2 border-b border-rose-50 dark:border-gray-700">
                        <p class="text-body-sm font-semibold text-gray-800 dark:text-gray-200"><%= t('navigation.quick_actions') %></p>
                      </div>
                      
                      <%= link_to new_wishlist_path, class: "flex items-center gap-3 px-4 py-2 text-sm text-gray-600 dark:text-gray-300 hover:bg-rose-50 dark:hover:bg-gray-700 hover:text-rose-600 dark:hover:text-rose-400 transition duration-200" do %>
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
                        </svg>
                        <div>
                          <div class="font-medium"><%= t('wishlists.index.new_wishlist') %></div>
                          <div class="text-xs text-gray-500"><%= t('navigation.quick_actions_descriptions.create_wishlist') %></div>
                        </div>
                      <% end %>
                      
                      <% if current_user.wishlists.any? %>
                        <div class="px-4 py-2 border-t border-rose-50 dark:border-gray-700">
                          <p class="text-xs text-gray-500 dark:text-gray-400 mb-2"><%= t('navigation.quick_actions_descriptions.add_to_existing') %></p>
                          <% current_user.wishlists.limit(3).each do |wishlist| %>
                            <%= link_to new_wishlist_wishlist_item_path(wishlist), class: "flex items-center gap-2 px-2 py-1.5 text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 hover:text-rose-600 dark:hover:text-rose-400 transition duration-200 rounded-lg" do %>
                              <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                              </svg>
                              <span class="truncate"><%= wishlist.name %></span>
                            <% end %>
                          <% end %>
                        </div>
                      <% end %>
                      
                      <%= link_to new_invitation_path, class: "flex items-center gap-3 px-4 py-2 text-sm text-gray-600 dark:text-gray-300 hover:bg-rose-50 dark:hover:bg-gray-700 hover:text-rose-600 dark:hover:text-rose-400 transition duration-200 border-t border-rose-50 dark:border-gray-700" do %>
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                        </svg>
                        <div>
                          <div class="font-medium"><%= t('navigation.invite_friend') %></div>
                          <div class="text-xs text-gray-500"><%= t('navigation.quick_actions_descriptions.connect_friends') %></div>
                        </div>
                      <% end %>
                    </div>
                  </div>
                </div>
              </div>
              
              <!-- Mobile Menu Button -->
              <div class="md:hidden flex items-center gap-2">
                <!-- Mobile + Button -->
                <div class="relative" data-controller="dropdown">
                  <button data-action="click->dropdown#toggle" class="p-2 bg-gradient-to-r from-rose-100 to-purple-100 dark:from-rose-900/50 dark:to-purple-900/50 text-rose-600 dark:text-rose-400 hover:from-rose-500 hover:to-rose-600 dark:hover:from-rose-600 dark:hover:to-rose-700 hover:text-white dark:hover:text-white transition-all duration-200 rounded-xl shadow-sm hover:shadow-md" title="<%= t('items.new') %>">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                    </svg>
                  </button>
                  
                  <div data-dropdown-target="menu" class="absolute right-0 mt-2 w-64 bg-white rounded-xl shadow-lg border border-rose-100 py-2 hidden z-50">
                    <div class="px-4 py-2 border-b border-rose-50">
                      <p class="text-sm font-semibold text-gray-800"><%= t('navigation.quick_actions') %></p>
                    </div>
                    
                    <%= link_to new_wishlist_path, class: "flex items-center gap-3 px-4 py-2 text-sm text-gray-600 hover:bg-rose-50 hover:text-rose-600 transition duration-200" do %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012 2v2M7 7h10"></path>
                      </svg>
                      <div>
                        <div class="font-medium"><%= t('wishlists.index.new_wishlist') %></div>
                        <div class="text-xs text-gray-500"><%= t('navigation.quick_actions_descriptions.create_wishlist') %></div>
                      </div>
                    <% end %>
                    
                    <% if current_user.wishlists.any? %>
                      <div class="px-4 py-2 border-t border-rose-50 dark:border-gray-700">
                        <p class="text-xs text-gray-500 mb-2"><%= t('navigation.quick_actions_descriptions.add_to_existing') %></p>
                        <% current_user.wishlists.limit(3).each do |wishlist| %>
                          <%= link_to new_wishlist_wishlist_item_path(wishlist), class: "flex items-center gap-2 px-2 py-1.5 text-sm text-gray-600 hover:bg-gray-50 hover:text-rose-600 transition duration-200 rounded-lg" do %>
                            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                            </svg>
                            <span class="truncate"><%= wishlist.name %></span>
                          <% end %>
                        <% end %>
                      </div>
                    <% end %>
                    
                    <%= link_to new_invitation_path, class: "flex items-center gap-3 px-4 py-2 text-sm text-gray-600 hover:bg-rose-50 hover:text-rose-600 transition duration-200 border-t border-rose-50" do %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                      </svg>
                      <div>
                        <div class="font-medium"><%= t('navigation.invite_friend') %></div>
                        <div class="text-xs text-gray-500"><%= t('navigation.quick_actions_descriptions.connect_friends') %></div>
                      </div>
                    <% end %>
                  </div>
                </div>
                
                <!-- Mobile notification bell -->
                <div class="relative" data-controller="notifications" data-notifications-user-id-value="<%= current_user.id %>">
                  <button data-action="click->notifications#toggle" class="relative p-2 text-gray-600 dark:text-gray-300 hover:text-rose-600 dark:hover:text-rose-400 transition duration-200 rounded-lg hover:bg-rose-50 dark:hover:bg-gray-700 cursor-pointer">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path>
                    </svg>
                    <% if current_user.unread_notifications_count > 0 %>
                      <span data-notifications-target="badge" class="absolute -top-1 -right-1 bg-rose-500 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center font-semibold">
                        <%= current_user.unread_notifications_count %>
                      </span>
                    <% else %>
                      <span data-notifications-target="badge" class="hidden absolute -top-1 -right-1 bg-rose-500 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center font-semibold">
                        0
                      </span>
                    <% end %>
                  </button>
                
                  <!-- Notification Dropdown (Mobile) -->
                  <div data-notifications-target="dropdown" class="hidden absolute right-0 mt-2 w-80 bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-rose-100 dark:border-gray-700 z-50">
                    <div class="px-4 py-3 border-b border-rose-50 dark:border-gray-700 flex items-center justify-between">
                      <h3 class="heading-sub text-gray-800 dark:text-gray-200"><%= t('notifications.bell_title') %></h3>
                      <% if current_user.unread_notifications_count > 0 %>
                        <%= link_to t('notifications.mark_all_read'), mark_all_as_read_notifications_path, 
                              data: { "turbo-method": :patch },
                              class: "text-xs text-rose-600 dark:text-rose-400 hover:text-rose-700 dark:hover:text-rose-300" %>
                      <% end %>
                    </div>
                    
                    <div data-notifications-target="list" class="max-h-96 overflow-y-auto">
                      <% if current_user.notifications.recent.limit(10).any? %>
                        <% current_user.notifications.recent.limit(10).each do |notification| %>
                          <div class="border-b border-gray-100 dark:border-gray-700 <%= 'bg-rose-50 dark:bg-rose-900/10' unless notification.read? %>">
                            <% unless notification.read? %>
                              <%= link_to mark_as_read_notification_path(notification), 
                                    data: { "turbo-method": :patch },
                                    class: "block px-4 py-3 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition duration-200" do %>
                                <div class="flex items-start gap-3">
                                  <div class="flex-shrink-0 w-2 h-2 mt-2 rounded-full bg-rose-500 dark:bg-rose-400"></div>
                                  <div class="flex-1">
                                    <p class="text-sm font-medium text-gray-800 dark:text-gray-100"><%= notification.localized_title %></p>
                                    <p class="text-xs text-gray-600 dark:text-gray-300 mt-1"><%= notification.localized_message %></p>
                                    <p class="text-xs text-gray-400 dark:text-gray-500 mt-1"><%= time_ago_in_words(notification.created_at) %> ago</p>
                                  </div>
                                  <% if notification.notification_type == 'invitation_received' && notification.data['invitation_token'] %>
                                    <div class="flex items-center gap-1">
                                      <%= link_to update_invitation_path(token: notification.data['invitation_token'], accept: true), 
                                            data: { "turbo-method": :patch },
                                            class: "p-1 bg-green-100 hover:bg-green-200 rounded transition duration-200",
                                            title: "Accept" do %>
                                        <svg class="w-3 h-3 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
                                        </svg>
                                      <% end %>
                                      
                                      <%= link_to update_invitation_path(token: notification.data['invitation_token'], accept: false), 
                                            data: { "turbo-method": :patch },
                                            class: "p-1 bg-red-100 hover:bg-red-200 rounded transition duration-200",
                                            title: "Decline" do %>
                                        <svg class="w-3 h-3 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M6 18L18 6M6 6l12 12"></path>
                                        </svg>
                                      <% end %>
                                    </div>
                                  <% end %>
                                </div>
                              <% end %>
                            <% else %>
                              <div class="px-4 py-3 hover:bg-gray-50 transition duration-200">
                                <div class="flex items-start gap-3">
                                  <div class="flex-shrink-0 w-2 h-2 mt-2 rounded-full bg-gray-300"></div>
                                  <div class="flex-1">
                                    <p class="text-sm text-gray-700"><%= notification.localized_title %></p>
                                    <p class="text-xs text-gray-500 mt-1"><%= notification.localized_message %></p>
                                    <p class="text-xs text-gray-400 dark:text-gray-500 mt-1"><%= time_ago_in_words(notification.created_at) %> ago</p>
                                  </div>
                                </div>
                              </div>
                            <% end %>
                          </div>
                        <% end %>
                      <% else %>
                        <div class="px-4 py-8 text-center text-gray-500">
                          <svg class="w-12 h-12 mx-auto mb-3 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path>
                          </svg>
                          <p class="text-sm"><%= t('notifications.no_notifications') %></p>
                        </div>
                      <% end %>
                    </div>
                    
                    <div class="px-4 py-2 border-t border-rose-50 dark:border-gray-700">
                      <%= link_to t('notifications.view_all'), notifications_path, class: "text-sm text-rose-600 hover:text-rose-700 font-medium" %>
                    </div>
                  </div>
                </div>
                
                <!-- User Avatar with Menu (Mobile) -->
                <button data-action="click->mobile-menu#toggle" class="relative flex items-center gap-1 p-1 rounded-lg hover:bg-rose-50 transition duration-200 cursor-pointer">
                  <% if current_user.avatar_url.present? %>
                    <img src="<%= current_user.avatar_url %>" alt="Avatar" class="w-8 h-8 rounded-full border-2 border-rose-200">
                  <% else %>
                    <div class="bg-gradient-to-r from-rose-100 to-purple-100 rounded-full w-8 h-8 flex items-center justify-center">
                      <span class="text-sm font-semibold text-rose-700">
                        <%= current_user.name&.first&.upcase || current_user.email&.first&.upcase %>
                      </span>
                    </div>
                  <% end %>
                  <!-- Chevron indicator -->
                  <svg class="w-3 h-3 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
              </div>
              
              <!-- User Menu (Desktop) -->
              <div class="hidden md:block relative" data-controller="dropdown">
                <button data-action="click->dropdown#toggle" class="flex items-center gap-2 text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 transition duration-200 p-1 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 cursor-pointer">
                  <% if current_user.avatar_url.present? %>
                    <img src="<%= current_user.avatar_url %>" alt="Avatar" class="w-8 h-8 rounded-full border-2 border-rose-200">
                  <% else %>
                    <div class="bg-gradient-to-r from-rose-100 to-purple-100 rounded-full w-8 h-8 flex items-center justify-center">
                      <span class="text-sm font-semibold text-rose-700">
                        <%= current_user.name&.first&.upcase || current_user.email&.first&.upcase %>
                      </span>
                    </div>
                  <% end %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
                
                <!-- Dropdown Menu -->
                <div data-dropdown-target="menu" class="absolute right-0 mt-2 w-64 bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-rose-100 dark:border-gray-700 py-2 hidden z-50 max-h-96 overflow-y-auto">
                  <div class="px-4 py-2 border-b border-rose-50 dark:border-gray-700">
                    <p class="text-sm font-medium text-gray-800 dark:text-gray-200"><%= current_user.name || "User" %></p>
                    <p class="text-xs text-gray-500 dark:text-gray-400"><%= current_user.email %></p>
                  </div>
                  <%= link_to edit_user_registration_path, class: "block px-4 py-2 text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200 cursor-pointer" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                      </svg>
<%= t('profile.edit.title') %>
                    </div>
                  <% end %>
                  <%= link_to notification_preferences_path, class: "block px-4 py-2 text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200 cursor-pointer" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path>
                      </svg>
<%= t('navigation.notification_preferences') %>
                    </div>
                  <% end %>
                  <%= link_to user_path(current_user), class: "block px-4 py-2 text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200 cursor-pointer" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
<%= t('navigation.view_public_profile') %>
                    </div>
                  <% end %>
                  <%= link_to connections_path, class: "block px-4 py-2 text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200 cursor-pointer" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                      </svg>
<%= t('navigation.connections') %>
                    </div>
                  <% end %>
                  
                  <div class="border-t border-rose-50 dark:border-gray-700 my-2"></div>
                  
                  <!-- Language Switcher -->
                  <div class="px-4 py-2">
                    <p class="text-xs text-gray-500 dark:text-gray-400 mb-2 uppercase tracking-wide">Language</p>
                    <% available_locales.each do |locale| %>
                      <%= link_to locale_switch_url(locale[:code]), 
                            class: "flex items-center gap-2 px-2 py-1.5 text-sm rounded-lg transition duration-200 cursor-pointer #{'bg-rose-50 dark:bg-rose-900/20 text-rose-700 dark:text-rose-400' if I18n.locale.to_s == locale[:code]} #{'text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700' unless I18n.locale.to_s == locale[:code]}" do %>
                        <span class="text-base"><%= locale[:flag] %></span>
                        <span><%= locale[:name] %></span>
                        <% if I18n.locale.to_s == locale[:code] %>
                          <svg class="w-3 h-3 ml-auto text-rose-600" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
                          </svg>
                        <% end %>
                      <% end %>
                    <% end %>
                  </div>
                  
                  <!-- Theme Switcher -->
                  <div class="border-t border-rose-50 dark:border-gray-700 px-4 py-2">
                    <p class="text-xs text-gray-500 dark:text-gray-400 mb-2 uppercase tracking-wide"><%= t('theme.toggle') %></p>
                    <div 
                      data-controller="theme"
                      data-theme-current-value="<%= user_signed_in? ? current_user.theme_preference : (session[:theme_preference] || 'system') %>"
                      data-theme-authenticated-value="<%= user_signed_in? %>"
                    >
                      <button 
                        data-action="click->theme#toggle"
                        data-theme-target="toggle"
                        class="flex items-center gap-2 px-2 py-1.5 text-sm rounded-lg transition duration-200 text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 w-full cursor-pointer"
                      >
                        <span data-theme-target="icon" class="text-base">🔄</span>
                        <span data-theme-target="text"><%= t('theme.system') %></span>
                      </button>
                    </div>
                  </div>
                  
                  <div class="border-t border-rose-50 dark:border-gray-700"></div>
                  
                  <%= link_to destroy_user_session_path, 
                        data: { "turbo-method": :delete },
                        class: "block w-full text-left px-4 py-2 text-sm text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition duration-200 cursor-pointer" do %>
                    <div class="flex items-center gap-2">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                      </svg>
<%= t('navigation.sign_out') %>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
            
          </div>
          
          <!-- Mobile Menu -->
          <div data-mobile-menu-target="menu" class="hidden md:hidden pb-4 max-h-[calc(100vh-120px)] overflow-y-auto border-t border-gray-100 dark:border-gray-700 bg-white dark:bg-gray-800">
            <div class="flex flex-col gap-3">
              <%= link_to t('navigation.wishlists'), wishlists_path, class: "text-gray-600 dark:text-gray-300 hover:text-rose-600 dark:hover:text-rose-400 font-medium transition duration-200 py-2 cursor-pointer #{request.path.match?(/\/wishlists/) ? 'text-rose-600 dark:text-rose-400' : ''}" %>
              <%= link_to t('navigation.connections'), connections_path, class: "text-gray-600 dark:text-gray-300 hover:text-rose-600 dark:hover:text-rose-400 font-medium transition duration-200 py-2 cursor-pointer #{request.path.match?(/\/connections/) ? 'text-rose-600 dark:text-rose-400' : ''}" %>
              
              <div class="border-t border-rose-100 pt-3 mt-3 flex flex-col gap-2">
                <%= link_to new_invitation_path, class: "bg-white border border-rose-200 text-rose-600 px-4 py-2 rounded-xl hover:bg-rose-50 transition duration-200 text-sm font-medium flex items-center gap-2 w-full justify-center cursor-pointer" do %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                  </svg>
  <%= t('navigation.invite_friend') %>
                <% end %>
                
                <%= link_to new_wishlist_path, class: "bg-gradient-to-r from-rose-500 to-rose-600 text-white px-4 py-2 rounded-xl hover:from-rose-600 hover:to-rose-700 transition duration-200 text-sm font-medium flex items-center gap-2 w-full justify-center shadow-sm cursor-pointer" do %>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                  </svg>
  <%= t('wishlists.index.new_wishlist') %>
                <% end %>
                
                <div class="mt-3 pt-3 border-t border-rose-100">
                  <div class="mb-3">
                    <p class="text-sm font-medium text-gray-800"><%= current_user.name || "User" %></p>
                    <p class="text-xs text-gray-500"><%= current_user.email %></p>
                  </div>
                  
                  <!-- Mobile Profile Links -->
                  <div class="mb-4 space-y-2">
                    <%= link_to edit_user_registration_path, class: "flex items-center gap-2 py-2 text-sm text-gray-600 hover:text-rose-600 transition duration-200" do %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                      </svg>
                      <%= t('profile.edit.title') %>
                    <% end %>
                    <%= link_to notification_preferences_path, class: "flex items-center gap-2 py-2 text-sm text-gray-600 hover:text-rose-600 transition duration-200" do %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path>
                      </svg>
                      <%= t('navigation.notification_preferences') %>
                    <% end %>
                    <%= link_to user_path(current_user), class: "flex items-center gap-2 py-2 text-sm text-gray-600 hover:text-rose-600 transition duration-200" do %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
                      <%= t('navigation.view_public_profile') %>
                    <% end %>
                  </div>
                  
                  <!-- Mobile Language Switcher -->
                  <div class="mb-4">
                    <p class="text-xs text-gray-500 mb-2 uppercase tracking-wide">Language</p>
                    <div class="flex gap-2">
                      <% available_locales.each do |locale| %>
                        <%= link_to locale_switch_url(locale[:code]), 
                              class: "flex items-center gap-2 px-3 py-2 rounded-lg text-sm transition duration-200 cursor-pointer #{'bg-rose-50 text-rose-700 border border-rose-200' if I18n.locale.to_s == locale[:code]} #{'text-gray-600 hover:bg-gray-50 border border-gray-200' unless I18n.locale.to_s == locale[:code]}" do %>
                          <span><%= locale[:flag] %></span>
                          <span><%= locale[:name] %></span>
                          <% if I18n.locale.to_s == locale[:code] %>
                            <svg class="w-3 h-3 ml-auto text-rose-600" fill="currentColor" viewBox="0 0 24 24">
                              <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
                            </svg>
                          <% end %>
                        <% end %>
                      <% end %>
                    </div>
                  </div>
                  
                  <!-- Mobile Theme Switcher -->
                  <div class="mb-4">
                    <p class="text-xs text-gray-500 mb-2 uppercase tracking-wide"><%= t('theme.toggle') %></p>
                    <div 
                      data-controller="theme"
                      data-theme-current-value="<%= user_signed_in? ? current_user.theme_preference : (session[:theme_preference] || 'system') %>"
                      data-theme-authenticated-value="<%= user_signed_in? %>"
                      class="flex gap-2"
                    >
                      <button 
                        data-action="click->theme#toggle"
                        data-theme-target="toggle"
                        class="flex items-center gap-2 px-3 py-2 rounded-lg text-sm transition duration-200 text-gray-600 hover:bg-gray-50 border border-gray-200"
                      >
                        <span data-theme-target="icon" class="text-base">🔄</span>
                        <span data-theme-target="text"><%= t('theme.system') %></span>
                      </button>
                    </div>
                  </div>
                  
                  <%= button_to t('navigation.sign_out'), destroy_user_session_path, method: :delete, 
                        class: "block w-full text-left py-2 text-sm text-rose-600 hover:text-rose-700 transition duration-200 border-0 bg-transparent font-medium" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <div class="bg-emerald-50 border border-emerald-200 text-emerald-700 px-4 py-3 rounded-xl flex items-start gap-3" role="alert">
          <svg class="w-5 h-5 text-emerald-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= notice %></span>
        </div>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <div class="bg-rose-50 border border-rose-200 text-rose-700 px-4 py-3 rounded-xl flex items-start gap-3" role="alert">
          <svg class="w-5 h-5 text-rose-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span><%= alert %></span>
        </div>
      </div>
    <% end %>
    
    <main class="<%= user_signed_in? ? 'pt-4' : 'pt-8' %> pb-16 flex-grow">
      <%= yield %>
    </main>
    
    <!-- Cookie Consent Banner -->
    <%= render 'shared/cookie_consent_banner' %>
    
    <!-- Footer - Only show on main app pages, not auth pages -->
    <% unless controller_name == 'sessions' || controller_name == 'registrations' || controller_name == 'passwords' || controller_name == 'confirmations' || controller_name == 'unlocks' %>
      <footer class="bg-white/80 backdrop-blur-md border-t border-gray-200 mt-auto">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="flex flex-col md:flex-row justify-between items-center gap-4">
          <!-- Logo and tagline -->
          <div class="flex items-center gap-2 text-center md:text-left">
            <span class="text-xl">💝</span>
            <div>
              <div class="font-display font-semibold bg-gradient-to-r from-rose-500 to-purple-600 bg-clip-text text-transparent text-lg">
                Wishare
              </div>
              <div class="text-xs text-gray-500 font-inter">
                <%= t('common.footer.tagline') %>
              </div>
            </div>
          </div>
          
          <!-- Legal links -->
          <div class="flex flex-wrap items-center justify-center gap-4 text-sm text-gray-600 font-inter">
            <%= link_to t('common.footer.terms_of_service'), terms_of_service_path, class: "hover:text-rose-600 transition duration-200" %>
            <span class="text-gray-300">•</span>
            <%= link_to t('common.footer.privacy_policy'), privacy_policy_path, class: "hover:text-rose-600 transition duration-200" %>
            <span class="text-gray-300 hidden sm:inline">•</span>
            <span class="text-gray-400 text-xs"><%= t('common.footer.copyright', year: Date.current.year) %></span>
          </div>
        </div>
      </div>
    </footer>
    <% end %>
  </body>
</html>
